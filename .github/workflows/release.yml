# Build and release Ansikten for all platforms
# Triggers on version tags (v1.0.0, v1.2.3, etc.)
#
# Build process:
# 1. Build Python backend with PyInstaller (platform-specific)
# 2. Copy bundled backend to frontend/resources/backend/
# 3. Build Electron app with electron-builder (includes backend)

name: Release

on:
  push:
    tags:
      - 'v*'

env:
  NODE_VERSION: '20'
  PYTHON_VERSION: '3.11'

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: backend/pyproject.toml

      - name: Install Python dependencies
        working-directory: backend
        run: |
          pip install --upgrade pip
          pip install ".[build]"

      - name: Build backend with PyInstaller
        working-directory: backend
        run: |
          pyinstaller ansikten-backend.spec
          ls -la dist/
          ls -la dist/ansikten-backend/

      - name: Prepare backend for Electron
        run: |
          mkdir -p frontend/resources/backend
          cp -r backend/dist/ansikten-backend/* frontend/resources/backend/
          chmod +x frontend/resources/backend/ansikten-backend

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Cache Electron
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Caches/electron
            ~/Library/Caches/electron-builder
          key: electron-macos-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: electron-macos-

      - name: Install Node dependencies
        working-directory: frontend
        run: npm ci

      - name: Set version from tag
        working-directory: frontend
        run: npm pkg set version="${GITHUB_REF_NAME#v}"

      - name: Build Electron app
        working-directory: frontend
        run: npm run build:workspace && npx electron-builder --mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-builds
          path: |
            frontend/dist/*.dmg
            frontend/dist/*.zip
          retention-days: 5

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: backend/pyproject.toml

      - name: Install Python dependencies
        working-directory: backend
        run: |
          pip install --upgrade pip
          pip install ".[build]"

      - name: Build backend with PyInstaller
        working-directory: backend
        run: |
          pyinstaller ansikten-backend.spec
          dir dist
          dir dist\ansikten-backend

      - name: Prepare backend for Electron
        run: |
          mkdir -p frontend/resources/backend
          cp -r backend/dist/ansikten-backend/* frontend/resources/backend/
        shell: bash

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Cache Electron
        uses: actions/cache@v4
        with:
          path: |
            ~/AppData/Local/electron/Cache
            ~/AppData/Local/electron-builder/Cache
          key: electron-windows-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: electron-windows-

      - name: Install Node dependencies
        working-directory: frontend
        run: npm ci

      - name: Set version from tag
        working-directory: frontend
        run: npm pkg set version="${GITHUB_REF_NAME#v}"
        shell: bash

      - name: Build Electron app
        working-directory: frontend
        run: npm run build:workspace && npx electron-builder --win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-builds
          path: |
            frontend/dist/*.exe
          retention-days: 5

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: backend/pyproject.toml

      - name: Install Python dependencies
        working-directory: backend
        run: |
          pip install --upgrade pip
          pip install ".[build]"

      - name: Build backend with PyInstaller
        working-directory: backend
        run: |
          pyinstaller ansikten-backend.spec
          ls -la dist/
          ls -la dist/ansikten-backend/

      - name: Prepare backend for Electron
        run: |
          mkdir -p frontend/resources/backend
          cp -r backend/dist/ansikten-backend/* frontend/resources/backend/
          chmod +x frontend/resources/backend/ansikten-backend

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Cache Electron
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/electron
            ~/.cache/electron-builder
          key: electron-linux-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: electron-linux-

      - name: Install Node dependencies
        working-directory: frontend
        run: npm ci

      - name: Set version from tag
        working-directory: frontend
        run: npm pkg set version="${GITHUB_REF_NAME#v}"

      - name: Build Electron app
        working-directory: frontend
        run: npm run build:workspace && npx electron-builder --linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-builds
          path: |
            frontend/dist/*.AppImage
            frontend/dist/*.deb
          retention-days: 5

  release:
    needs: [build-macos, build-windows, build-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f -name "*"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          generate_release_notes: true
          files: |
            artifacts/**/*.dmg
            artifacts/**/*.zip
            artifacts/**/*.exe
            artifacts/**/*.AppImage
            artifacts/**/*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
