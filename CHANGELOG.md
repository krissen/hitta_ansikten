# Changelog

All notable changes to this project are documented in this file.

This changelog is initialized from git commit history after `v1.0.0` and can be refined before upcoming releases.

## [Unreleased]

### Fixed
- **Matchningströsklarna kördes mot fel distansmetrik — hela systemet matchade extremt löst.** De platta config-nycklarna `match_threshold`/`ignore_distance`/`hard_negative_distance` var euklidiska (dlib-eran) värden, men InsightFace använder cosinus-distans. På disk låg `match_threshold: 0.6` kvar — cosinus-distans 0,6 = cosinus-*likhet* 0,4, dvs nästan vad som helst matchade. CLI-vägen (`core/matching._get_backend_thresholds`) lät den stala platta nyckeln överskugga de korrekta cosinus-defaultsen, och API-vägen (`detection_service._determine_match_case`) läste de platta nycklarna direkt utan att ens gå via backend-trösklarna. **Fix:** `backend_thresholds.<backend>` är nu enda sanningskälla för match/ignore/hard-negative-trösklar; båda vägarna går genom samma `_get_backend_thresholds`, och platta toppnivånycklar konsulteras aldrig (de är fel-metrik). InsightFace matchar nu på de kanoniska cosinus-defaultsen (match 0,4 / ignore 0,35 / hard-negative 0,32). En engångsmigrering vid config-laddning (`core/config._migrate_config`, `config_version: 2`) skriver in `backend_thresholds.insightface` med korrekta värden och tar bort de gamla platta nycklarna för konfigurationer som saknar ett insightface-block — de felaktiga 0,6/0,5-värdena kopieras **inte** framåt. Migreringen är idempotent och rör inte en config som redan har ett insightface-block. Mjuklandning för träffsäkerheten: namn autofylls bara under matchtröskeln (0,4); ansikten i det osäkra bandet strax över tröskeln autofylls inte men den närmaste personen syns fortfarande i alternativlistan i granskningsvyn (ingen ny match_case, ingen frontend-ändring).

## [1.4.1] - 2026-07-08

### Fixed
- **Landningssidan: stegknapparna växte efter B7 och tvingade fram skroll.** `.startup-landing-step` har alltid deklarerat `font-size: var(--font-lg)` + tjock padding, men regeln var vilande — legacy `.btn-action` (theme.css, buntad efter modul-CSS) vann kaskaden. B7:s byte till delade `<Button>` flippade buntordningen och väckte regeln. Den vilande storleksregeln är borttagen; primitivens kompakta storlek gäller och allt ryms utan skroll.

### Changed
- **Landningssidan visar nu arbetsflödeslogiken.** De fem första posterna är arbetsflödets steg i ordning — nu under rubriken "Arbetsflöde", numrerade 1–5 och som primärknappar; stödvyerna ligger kvar under "Verktyg" som secondary. Undertexten uppdaterad ("Följ stegen i ordning, eller öppna ett verktyg direkt."). Ordinalen är dold för skärmläsare (knappnamnet förblir rent, t.ex. "Importera").

## [1.4.0] - 2026-07-08

### Fixed
- **Windows-release-bygget: Tailwind-steget kunde inte spawna `npx`.** `spawnSync('npx', …)` hittar inte `npx.cmd` på Windows utan shell (Node vägrar `.cmd` utan shell sedan CVE-2024-27980-härdningen) — release-workflowns Windows-jobb föll med "Tailwind build failed". Byggskriptet kör nu Tailwind-CLI:ns JS-entry direkt via node (`process.execPath`) — ingen npx, ingen shell, ingen kvoteringsrisk för sökvägar med mellanslag; alla plattformar samma väg.

### Added
- **Temaförfining: WCAG AA-kontrastpass + fokusring-token (fas C1).** Ljusa temats fyra statusfärger som text föll under AA på den beiga grundytan (2,6–3,2:1) — `--color-success-text`/`-warning-text`/`-error-text` mörkades med bevarad kulörton till ≥4,6:1 (`#174f07`/`#733000`/`#88160c`), och ny `--color-info-text` (`#0c466f` ljust / `#87ceeb` mörkt — nyckeln finns i BÅDA teman) ersätter `--color-info` där info visas som text (`.text-info`, Alert info-variant, autocomplete-markering; info-hue:n på beige låg på 1,8:1). `--overlay-bg` (ljust) mörkades till `#846837` så overlay-texten når 4,6:1, och mörka temats `--text-tertiary` höjdes till `#7f7d75` (4,3→4,7:1). Ny `--focus-ring`/`--focus-ring-offset`-token; delade primitiver (knappar, autocomplete, modal-innehåll) visar nu en enhetlig accentring på `:focus-visible`, och FlexLayout-flikarnas handrullade ring återanvänder tokenen. Samtliga status/text-par klarar nu AA i båda teman (mätt); [theming.md](docs/dev/theming.md)-tabellerna uppdaterade.
- **Övriga moduler + städ: sista fas B-migreringen (fas B7).** Slutstädning av kvarvarande moduler och borttagning av legacy-alias. **i18n:** `PlayerCountModule` var bara delvis i18n:ad — hela modulen flyttades till `playerCount`-namespacet (titel, kö-status, summering, räkneoptioner, uteslutningseditor, tabellrubriker, spark-/rad-titlar, exkluderade-grupper, per-match-sektioner) med **oförändrad ordalydelse**; `StatisticsDashboard` fick sin magiska sentinel-sträng `'Ignored'` ersatt av en `isIgnored`-flagga (etiketten kom redan från katalogen — strängen kunde aldrig läcka till UI, men beroendet var bräckligt). **Tysta fel → feedback:** `ImageViewer`s tysta bildinläsningsfel (bara `debugError`) ytar nu en `type:'error'`-toast (`imageViewer.loadFailed`, svenska via namespacet) så en misslyckad laddning inte lämnar vyn tyst tom; `LogViewer`s handrullade "Kopierat"-flagga (`copyFeedback`-state + `setTimeout`) ersattes av en `success`-toast (och en `error`-toast vid klippbordsfel). **Delad util:** `OriginalView`s naiva `'file://' + jpgPath`-konkatenering (bröt på filnamn med mellanslag/`#`/`?`/`[]`) → delad `toFileUrl()` från `shared/fileUrl.js`. **Knapp-triage:** alla kvarvarande råa `.btn-*`-klasser migrerade till `Button`/`IconButton` (`LogViewer`, `StatisticsDashboard`, `PlayerCountModule`, `InputBar`, `StartupLanding`); de inline `.status-message`-bannrarna i `StatisticsDashboard` + `PlayerCountModule` → delad `<Alert variant="error">`. **Klickbar rad-a11y:** `PlayerTable`-radernas mus-endast `onClick` fick husets `role="button"` + `tabIndex` + Enter/Space + `aria-label` ([accessibility.md §2](docs/dev/accessibility.md), paritet med Cullings statspanel). **Keyboard-scope-beslut** (Nagelfar #177): `data-keyboard-scope="isolated"` satt på `FileQueueModule` (Review-companion — skölda Reviews snabbtangenter när fokus är i kön; egen `n`/`p` opåverkad), `PlayerCountModule` och `RenameNefModule` (formulärtunga — hindra `Enter`/bokstavsläckage till en synlig Review-panel); `CullingModule` **avsiktligt inte** isolerad (den äger tangenter via capture-fas gate:ad på aktiv tabset). Kontraktet + tabell per modul dokumenterat i [accessibility.md §7](docs/dev/accessibility.md). **Roving tabindex** (Nagelfar #174): ett gemensamt mönster beslutades och implementerades för den värsta listan, `FileQueueItem` (100+ rader) — en enda roving-target-rad är tabbar (`tabIndex 0`, övriga `-1`), pilar flyttar markören mellan rader; reducerar ~4N tab-stops till ~4 ([accessibility.md §2a](docs/dev/accessibility.md)). Preferences/ThemeEditor-sidonaven lämnad som preciserad ROADMAP-punkt. **Chips-utvärdering:** `PlayerCount`s uteslutnings-chips **behölls** (inte migrerade till delad `<Autocomplete>`) — kontrollen är ett rent chip-fält med fritextinmatning utan förslagskälla; att koppla in combobox-primitiven hade krävt en ny namnförslags-datakälla (nya personer), en ny funktion bortom städ och YAGNI-gränsen. **Alias-städning:** legacy `.btn-action`/`.btn-secondary`/`.btn-danger`/`.btn-icon`- och `.status-message`-KLASSREGLERNA borttagna ur `theme.css` (grep bekräftar noll JSX-användningar kvar); `--btn-secondary-hover-bg`/`--btn-icon-hover-bg`-tokens behållna (konsumeras av primitiverna). theming.md-alias-noteringen borttagen. 473 tester gröna; ingen visuell omdesign.
- **Review-slutförande: `useConfirm()`, i18n-rester, explicit keyboard-scope-kontrakt (fas B6).** Sista modulmigreringen — den mest tangentbordskänsliga. `useReviewKeyboard`-flödena (Enter/siffror/`i`/`r`/pilar/Esc/`x`/`m`/⌘Z/⌘Backspace), FaceCard-autocompleten och aktiv-tabset-gaten är **beteendemässigt oförändrade**. **Bekräftelse:** `ReviewModule.discardChanges` (Esc med osparade ändringar) gick från native `window.confirm()` till promise-baserad `await confirm({ variant:'danger' })`; pending-antalet snapshotas före `await` så en dubbel-Esc no-op:ar rent och prompten är konsekvent med det den visar. **Feedback:** `saveAllChanges`-felet ytar nu **också** en `type:'error'`-toast utöver den kvarstående inline-statusen (auto-spar-vägen var annars tyst); de avsiktligt icke-fatala tysta felen (`markReviewComplete`, `loadPeopleNames`, suffix-prefill) behålls som debug-loggning. **Knapp-triage:** "Lägg till namn manuellt" (rå `.btn-secondary`) → `Button variant="secondary"` (layout-klassen `.add-manual-name-btn` behålls); review-dialogerna använde redan `Button` sedan A2. **i18n:** hårdkodade svenska strängar → `t()` — FaceCard-badges (`review.badges.manual`/`.probableIgnore`/`.uncertainIgn`/`.uncertainName`, konsoliderat "Okänd" via `review.unknown`), tvilling-särskiljning (`review.twinDisambig.label`/`.title`), kö-barens tooltip (`review.queueBar.title`), ny `startupLanding`-namespace (`StartupLanding` titel/underrubrik/verktyg/kort-hint), och `StartupStatus` i18n-timing-bugg (`COMPONENT_LABELS`/`INITIAL_STATUS` anropade `t()` vid modulladdning → resolvas nu i render via `createInitialStatus()` + labels i komponentkroppen). **Keyboard-scope-kontrakt:** `useReviewKeyboard`s hårdkodade `.closest('.log-viewer, .preferences-module, .database-management, .theme-editor')`-selektorlista (som avgör vilka moduler som blockerar review-genvägar) ersatt av ett explicit, grep-bart dataattribut — blockerande modulrötter märks med `data-keyboard-scope="isolated"` och hooken frågar `activeEl.closest('[data-keyboard-scope="isolated"]')`. Detta **åtgärdar en latent bugg**: listans `.database-management` hade redan ruttnat (DatabaseManagements rot är `.db-management` sedan B1), så den modulen sköldades inte längre; attributet är på alla fyra rötterna (LogViewer, Preferences, ThemeEditor, DatabaseManagement) så täckningen är korrekt igen. **Test:** fence-testet (`reviewModule.test.jsx`, 20 st) står grönt — discard-gate-testet bytte från `window.confirm`-spion till en mockad `useConfirm()`-stub (samma B4-mönster, beteendet oförändrat). 473 tester gröna; ingen visuell omdesign.
- **Migrering av `ImportModule` + `RenameNefModule` till delade primitiver (fas B5).** Båda modulerna var tidigare helt o-i18n:ade och byggde på rå `<button className="btn-*">`-markup. **i18n:** två nya namespaces, [`i18n/sv/import.js`](frontend/src/i18n/sv/import.js) och [`i18n/sv/renameNef.js`](frontend/src/i18n/sv/renameNef.js), registrerade i [`i18n/sv/index.js`](frontend/src/i18n/sv/index.js); alla hårdkodade svenska strängar flyttade verbatim till `t('import.…')`/`t('renameNef.…')` (omformulering är fas C). Den reserverade nyckeln `import:` fungerar som objektnyckel i CommonJS-katalogen (verifierat) — inget alias behövdes. **Knapp-triage** (theming.md, max en primary per vy): Import "Importera" (kör) → `Button variant="primary"` (enda primary), "Uppdatera"/"Välj…" → `secondary`; RenameNef "Byt namn" (execute) → `primary` (enda primary), "+ Mapp"/"Förhandsgranska" → `secondary`. **Feedback:** de inline `.status-message error`-bannrarna ersätts av delad `<Alert variant="error">` (`role="alert"`); en transient toast (`success`, eller `warning` vid delfel) kvitterar avslutad överföring/namnbyte medan det strukturerade resultatpanelet (överhoppade/fel/utmatning) står kvar för inspektion. **EmptyState:** RenameNefs tomlägen ("Välj en mapp och tryck Förhandsgranska", "Inget att döpa om") renderas via `<EmptyState>`; Imports "Inget minneskort hittat" hålls kvar som inline fältstatus (fältnivå, inte modul-tomt — mål- och överföringsfälten är fortsatt relevanta). **A11y:** RenameNefs mapp-chip ×-knapp (tidigare naken `<button>×` utan tillgängligt namn) blir `<IconButton icon="close" variant="ghost">` med svensk `aria-label` ("Ta bort {mapp}"), kompakt hållen inuti chippen via `.rename-nef-chip-x`-scopad storlek; glob-`<input>` fick `aria-label` (dess UA-default-border lämnad orörd per Tailwind-grundens kontrakt). **Bekräftelser — medvetet inga:** RenameNef execute är preview-gate:ad (användaren har redan sett exakt mappning), skriver aldrig över befintliga mål och är reversibel → en confirm efter en explicit förhandsgranskning vore redundant friktion. Import kör en explicit, tydligt märkt överföring (move kopierar innan källan tas bort; inget raderas oåterkalleligt) → ingen confirm; confirms reserveras för genuint destruktiva/oåterkalleliga åtgärder (töm papperskorg, radera förval). 473 tester gröna; ingen visuell omdesign.
- **Migrering av `FileQueueModule` + `TrashPanel` till delade primitiver (fas B4).** Filkö- och papperskorgsmodulerna byter UI-skalet mot fas A-verktygslådan; kö-reducern, förbehandlings- och namnbyteslogiken (H8/H9) är orörda. **Bekräftelser:** filkö-namnbytet (`useNefRename`) gick från native `window.confirm()` till promise-baserad `await confirm({…})` med **bevarad** preferensgate (`requireConfirmation`); `TrashPanels` "töm papperskorgen" **saknade bekräftelse helt** och fick nu en `variant:'danger'`-dialog ("Radera N filer permanent? …") som gäller både töm-allt och töm-filtrerat, med filantal i strängen. **Feedback:** `TrashPanels` inline-`"Fel:"`-banner (oklassad `.status-message`) ersätts av den delade `<Alert variant="error">` (avfärdbar, `role="alert"`); tomlägena (filkön tom, papperskorgen tom, inga filer av typen) renderas via `<EmptyState>`. **Knapp-triage** (theming.md, max en primary per vy): filköns "Starta" → `Button variant="primary"` (enda primary; visas bara i viloläge, ömsesidigt uteslutande med "Hoppa över"), "Byt namn"/"Hoppa över" → `secondary`, "Rensa valda/klara/alla" → `ghost` (rensning är inte destruktiv); rubrikens verktygsknappar (lägg till filer/mapp, sortera, auto-avancera) + filter-stäng → `<IconButton>`; papperskorgens "Töm" → `danger`, "Återställ" → `secondary`. **A11y:** varje filköpost (`FileQueueItem`) fick husets `role="button"` + `tabIndex={0}` + Enter/Space-aktivering (laddar filen, mus-motsvarighet är dubbelklick) med svensk `aria-label` (filnamn + status); aktivering claim:ar bara Enter/Space när raden själv har fokus (`e.target === e.currentTarget`) så nästlade kryssrutor/knappar inte dubbeltriggar, och modulens `n`/`p`-navigering på document-lyssnaren är oförändrad. Radens ×-ta-bort (tidigare naken `<button>×` utan tillgängligt namn) och 🔄-behandla-om blir `<IconButton>` med svensk `aria-label`; deras slimmade hover-avslöjade utseende bevaras via `.file-item`-scopad CSS som vinner över `.icon-btn`-basen. **i18n:** ny `fileQueue.item.ariaLabel` och `trash.emptyConfirm`/`emptyConfirmTitle`/`emptyConfirmAction`. Fence-testerna (`fileQueueModule.test.jsx`, 30 st) står gröna — de tre namnbytes-gate-testerna bytte från `window.confirm`-spion till en mockad `useConfirm()`-stub (beteendet oförändrat, bara confirm-mekanismen). 473 tester gröna; ingen visuell omdesign.
- **Migrering av Culling-familjen till i18n + delade primitiver (fas B3).** Culling-modulen (`CullingModule.jsx`, `culling/FilterBar.jsx`, `culling/StatsPanel.jsx`, `culling/ContextMenu.jsx`, `culling/useCullingPreview.js`, `CullingGrid.jsx`) fick ett eget i18n-namespace [`i18n/sv/culling.js`](frontend/src/i18n/sv/culling.js): **alla** tidigare hårdkodade svenska strängar (filterbar, filtyps-/spelarval, glob-placeholder, Visa/Rutnät/Papperskorg, listrubrik "{count} bilder", tomlägen, statspanelens rubriker/kolumner/exkluderade grupper/radtitlar, kontextmenyn, namn-chip-titlar, ⌘↵-hinten, gridens aria-label "Miniatyröversikt", samt trash-/Lightroom-/preview-felmeddelanden) resolvas nu via `t('culling.…')` med **oförändrad ordalydelse** (omformulering är fas C). **Egen modal → delad `<Modal>`:** den handrullade "Osparade namnändringar"-dialogen (fast overlay-div) ersattes av husets `<Modal>` (native `<dialog>` i top-layer + backdrop) med `<Button>`-footer; dess särpräglade tangentsemantik (⌘↵ spara, ↵ kasta, Esc avbryt) ligger kvar i den befintliga capture-fas-effekten som **oförändrad** äger de tangenterna före Modal/andra moduler — fence-testerna (#106/#155, 20 tester) gröna. **Feedback:** de två inline `.status-message`-felraderna (modulens `error` samt preview-fel) → persistent `<Alert variant="error">` (avvisbar för toppfelet). **A11y:** loupe-fillistan fick husets listbox/`aria-activedescendant`-mönster (`role="listbox"` + `role="option"`/`aria-selected` per rad, se [accessibility.md §1](docs/dev/accessibility.md)) i paritet med `CullingGrid`; de klickbara statspanel-raderna fick `role="button"` + `tabIndex` + Enter/Space (§2) — capture-fas-Enter/Esc-handlern yield:ar nu för ett fokuserat `role="button"` så en tangent-Enter där filtrerar i stället för att starta en namnändring. **Knapp-triage** (se [theming.md](docs/dev/theming.md)): Visa → `Button variant="primary"` (enda primary i baren), + Mapp → `secondary`, Rutnät/Papperskorg → `secondary`-toggles som signalerar på-läge via `aria-pressed` (accent-utseende via scopad CSS); mapp-chipparnas ×-knapp → `<IconButton icon="close" variant="ghost">` (svensk `aria-label`). **EmptyState** för tomlägena "Välj mapp…/Visa", "Inga bilder", "Ingen bild vald" och konverterings-/inläsnings-platshållarna. Ersatt legacy-CSS borttagen i samma PR (`.culling-confirm-backdrop/-title/-actions`, gamla `.culling-chip-x`-reglerna). `CullingGrid` rördes minimalt (bara i18n av aria-label). Beteende i övrigt oförändrat; 473 frontend-tester gröna.
- **Migrering av `PreferencesModule` + `ThemeEditor` till delade primitiver (fas B2).** Bägge inställningsmodulerna använder nu husets `useConfirm()`, toast-systemet och `Button`/`IconButton` i stället för rå markup och tysta fel. **Bekräftelser:** Preferences "Återställ inställningar" och "Rensa cache" (native `confirm()`) → promise-baserad `await confirm({…})`; de tidigare **obekräftade** destruktiva ThemeEditor-åtgärderna "Ta bort förval" och "Återställ tema" fick nya `variant:'danger'`-dialoger med tydliga svenska strängar (vad som raderas/återställs; delete namnger förvalet, reset klargör att sparade förval inte påverkas). Alla fyra guardas mot dubbelklick via en `confirmBusyRef`, och confirm avgörs före state-ändringar. Delete läser dessutom senaste presets via funktionell `setState` så en stale closure inte återuppväcker ett borttaget förval. **Tysta fel → feedback:** de tre tidigare tyst svalda felen (Preferences: spara papperskorgsgräns, rensa cache; ThemeEditor: importera förval) ytar nu en `type:'error'`-toast med begripligt svenskt meddelande (vad hände + nästa steg); debug-loggningen behålls. **i18n-timing-bugg:** Preferences `SECTIONS`-konstanten anropade `t()` vid modulladdning (före ev. locale-init) — sektionsetiketterna resolvas nu i render via `sectionLabel(id)` från en ren id-lista (ThemeEditor lagrade redan i18n-*nycklar* och resolvade i render, ingen ändring behövdes där). **Knapp-triage:** Preferences Spara → `Button variant="primary"` (enda primary i vyn), Återställ/Rensa cache/Återställ felsökningskategorier → `secondary`; ThemeEditor Spara förval → `primary` (enda primary), Exportera/Importera/Återställ → `secondary`, och förvalens ×-raderaknapp → `<IconButton icon="close" variant="danger">` (röd glyf, röd fyllning vid hover — ersätter den handrullade `.custom-preset button`-CSS:en). **A11y:** sidomenyernas klickbara `<li>`/`<span>` (Preferences-sektioner, ThemeEditor-kategorier och -förval) fick husets `role="button"` + `tabIndex={0}` + Enter/Space-aktivering (se [accessibility.md §2](docs/dev/accessibility.md)); Preferences-fältkomponenterna hade en död `id`-prop som aldrig applicerades — de kopplar nu label↔kontroll med `useId()` (`htmlFor`/`id`) och hint↔kontroll via `aria-describedby`. FormField-primitiven (planerad i B1) fanns inte på dev vid start, så kopplingen gjordes manuellt. ThemeEditors runtime-mekanik (setProperty-overrides, presets i localStorage, temavariabler) är **beteendemässigt oförändrad** — bara UI-skalet migrerades. Ersatt legacy-CSS borttagen i samma PR (`.preset-list .custom-preset button`, `.preset-buttons .btn-secondary` → `.btn`).
- **Delad `<Autocomplete>`-combobox-primitiv + FaceCard-migrering (fas A4).** Ny [`components/shared/Autocomplete.jsx`](frontend/src/renderer/components/shared/Autocomplete.jsx) är en presentationskomponent för autocomplete-fält: konsumenten beräknar och rankar `options` från sitt eget query-state och äger `value` (visat värde), medan primitiven äger öppna/stäng-läge, det markerade förslaget och all combobox-ARIA. Full ARIA-modell enligt husets listbox/`aria-activedescendant`-mönster (se [accessibility.md §1a](docs/dev/accessibility.md)): input `role="combobox"` + `aria-expanded`/`aria-controls`/`aria-activedescendant`/`aria-autocomplete="list"`, listan `role="listbox"`, options `role="option"` + `aria-selected` — DOM-fokus stannar i inputen. Props: `value`, `options`, `getOptionLabel?`, `getOptionKey?`, `onInputChange`, `onSelect`, `onHighlight`, `onEscape`, `onKeyDown?`, `placeholder`, `inputRef`, `inputClassName`, `ariaLabel`, `maxSuggestions?`, `openOnFocus`, `selectOnEnter`, `navigateWithTab`. Tangentbord: pil upp/ned flyttar markeringen (wrap), Enter väljer markerat (avstängbart via `selectOnEnter={false}` när en dokument-lyssnare äger Enter), Esc stänger + blur + `stopPropagation()` (läcker inte till app-nivåns Esc), Tab kan navigera (`navigateWithTab`). Markeringen nollställs på faktiskt *innehålls*-byte i optionslistan (signatur, inte array-identitet), så en highlight-preview inte klobbrar markeringen. Autocomplete-CSS (`.autocomplete-wrapper`/`.autocomplete-dropdown`/`.autocomplete-item`) flyttad från `ReviewModule.css` till [`shared.css`](frontend/src/renderer/components/shared/shared.css) (oförändrade tokens → identiskt utseende); dropdownen portalas till `<body>` och positioneras via `useDropdownPosition`. [`review/FaceCard.jsx`](frontend/src/renderer/components/review/FaceCard.jsx) migrerad till primitiven med **exakt oförändrat publikt beteende** — samma tangentflöden (`r`, siffror, Enter, Esc), prefix-före-substräng-rankning (kvar i [`review/nameAutocomplete.js`](frontend/src/renderer/components/review/nameAutocomplete.js)), fritextnamn och förslagsval; Enter faller fortfarande igenom till `useReviewKeyboard`s `confirmEnter` (`selectOnEnter={false}`) och Tab navigerar förslag (`navigateWithTab`). Nya Vitest-tester i [`frontend/tests/shared/Autocomplete.test.jsx`](frontend/tests/shared/Autocomplete.test.jsx) (pilnav → aria-activedescendant, Enter-val, Esc utan läckage, fritext vs optionsval, ARIA, maxSuggestions); befintliga review-fence-tester oförändrade och gröna. Exporterad via `components/shared/index.js`. Database/PlayerCount-migreringarna är fas B.
- **Statusfeedback-primitiver: uppgraderad toast + `<Alert>`, flyttad `<ProgressBar>` och ny `<EmptyState>` (fas A3).** [`context/ToastContext.jsx`](frontend/src/renderer/context/ToastContext.jsx) fick en **bakåtkompatibel** signatur — både `showToast(message, 'error', 4000)` (legacy positional) och `showToast(message, { type, duration })` fungerar (varianter `success`/`error`/`info`/`warning`; duration klampas fortfarande till min 3s, `null`/utelämnat beter sig som förr). ToastContainer är nu en `role="status"`/`aria-live="polite"`-region och error-toasts annonseras assertivt via `role="alert"`; varje toast har en `IconButton`-stängknapp (svensk `aria-label` via `t('common.dismiss')`) i stället för den gamla klickbara diven. Varianttyperna behåller exakt dagens utseende (solida `--color-*`-fyllningar + `--text-inverse`). Ny [`components/shared/Alert.jsx`](frontend/src/renderer/components/shared/Alert.jsx) (`variant="error|warning|info|success"`, valfri `onDismiss`, valfri `icon`) — persistent inline-statusyta mot `--color-*-bg/-text/-*`-tokens, matchar legacy `.status-message` så fas-B-migreringar blir förlustfria; `error` → `role="alert"`, övriga → `role="status"`. `components/ProgressBar.jsx` flyttad till [`components/shared/ProgressBar.jsx`](frontend/src/renderer/components/shared/ProgressBar.jsx) (CSS med) och fick `role="progressbar"` + `aria-valuenow/-valuemin/-valuemax` (indeterminate utelämnar `aria-valuenow`) och en `ariaLabel`-prop; `LoadingOverlay` är nu en `role="status"`/`aria-live="polite"`-region. Alla importställen uppdaterade (`ImportModule`, `ImageViewer`); ingen re-export-skugga kvar. Ny [`components/shared/EmptyState.jsx`](frontend/src/renderer/components/shared/EmptyState.jsx) (`icon?`/`title`/`description?`/`action?`) matchar visuellt de befintliga ad hoc-tomlägena (48px-ikon + rubrik + hint). Allt exporterat via `components/shared/index.js`. Vitest-tester i [`frontend/tests/shared/`](frontend/tests/shared/) (Toast, Alert, ProgressBar, EmptyState). Mönstren dokumenterade i [accessibility.md](docs/dev/accessibility.md) (de tidigare "planned — A1/A3"-markörerna för IconButton/toast/Alert är nu levererade) och [theming.md](docs/dev/theming.md). Ingen modulmigrering (status-message→Alert/toast är fas B).
- **Delad `<Modal>`-primitiv på native `<dialog>` + promise-baserad `useConfirm()` (fas A2).** Ny [`components/shared/Modal.jsx`](frontend/src/renderer/components/shared/Modal.jsx) bygger på native `<dialog showModal()>` för plattformens **top-layer** (alltid ovanför FlexLayout-tabsets, inga z-index-krig), fokusfälla, `::backdrop` och `Esc` (native `cancel`-event) gratis. API: `<Modal open onClose title footer size="sm|md|lg" initialFocusRef closeOnBackdrop>`; `open` driver `showModal()`/`close()`, backdrop-klick och Esc → `onClose`, `title` kopplas till `aria-labelledby`. **Tangentbordsskydd:** appens globala genvägslager (t.ex. `useReviewKeyboard`) lyssnar på *native* `document`-keydown, och en native `<dialog>` stoppar inte keydown-bubbling — `Modal` gör därför `stopPropagation()` i en enda `onKeyDown` på dialog-elementet (React-syntetisk `stopPropagation()` stoppar även det underliggande native-eventet, så bubblingen når aldrig `document`-lyssnarna). Det ersätter det per-komponent-`stopPropagation` de gamla review-dialogerna själva bar. Retro-skarp stil i [`shared.css`](frontend/src/renderer/components/shared/shared.css) mot tokens (`--radius-sm`, `--border-strong`, `--bg-secondary`, `--shadow-lg`, dämpad `::backdrop`; öppningsanimation via `--motion-duration-base`, av under `prefers-reduced-motion`). Ny [`context/ConfirmContext.jsx`](frontend/src/renderer/context/ConfirmContext.jsx) med `ConfirmProvider` (monterad bredvid `ToastProvider`) och `useConfirm()` → `await confirm({ title?, message, confirmLabel?, cancelLabel?, variant: 'default'|'danger' })`: Enter bekräftar, Esc/backdrop/unmount resolvar `false`, `danger`-varianten renderar bekräfta-knappen som `Button variant="danger"`. Review-dialogerna [`review/ConfirmDialog`](frontend/src/renderer/components/review/ConfirmDialog.jsx) och [`review/SuffixDialog`](frontend/src/renderer/components/review/SuffixDialog.jsx) porterade till Modal-basen (oförändrade publika props + tangentbordsflöde) och använder nu `Button`/`Kbd`-primitiverna; död `.confirm-overlay`/`.confirm-dialog`/`.confirm-buttons`-CSS i `ReviewModule.css` ersatt av Modal-klasserna. Vitest-tester i [`frontend/tests/shared/`](frontend/tests/shared/) (Modal + useConfirm) plus omriktade selektorer i `confirmDialog.test.jsx`; jsdom saknar `showModal`/`close` så [`frontend/tests/setup.js`](frontend/tests/setup.js) polyfillar dem (registrerat via `vitest.config.js` `setupFiles`). Mönstret (top-layer, keydown-skydd, `useConfirm`) dokumenterat i [accessibility.md](docs/dev/accessibility.md) och Modal-klasskontraktet i [theming.md](docs/dev/theming.md). Ingen migrering av `confirm()`-anrop i moduler (det är fas B).
- **A11y-grund: reduced-motion + ARIA-live på globala statusytor (noll visuell förändring utan reduced-motion-preferens).** Ett globalt `@media (prefers-reduced-motion: reduce)`-block sist i [`theme.css`](frontend/src/renderer/theme.css) neutraliserar all animation, alla transitions och CSS-smooth-scroll för användare som begärt minskad rörelse; utan preferensen är utseendet oförändrat. Nya invarianta tokens `--motion-duration-fast`/`--motion-duration-base` (i synk med `--transition-*`) för framtida JS-vänliga/komponerade animationer. JS-styrd smooth-scroll (som CSS inte når) gate:as via ny delad util [`shared/motion.js`](frontend/src/renderer/shared/motion.js) (`prefersReducedMotion()` + `scrollBehavior()`) på de tre `scrollIntoView({behavior:'smooth'})`-ställena: `FileQueueModule` (aktiv-rad-scroll + current-file-bar-klick) och `ReviewModule` (aktivt ansikte). `CullingGrid`:s scroll använder redan instant `block:'nearest'`. ARIA-live lagt på globala ytor: [`StartupStatus`](frontend/src/renderer/components/StartupStatus.jsx) statuslista (`role="status"`/`aria-live="polite"`) och [`ConnectionStatus`](frontend/src/renderer/components/ConnectionStatus.jsx) (`role="alert"`/`assertive` vid tappad backend, nedgraderas till `status`/`polite` för "ansluter"-läget). Husets a11y-mönster (tangentbordsnav med `CullingGrid` som mall, klickbara icke-knappar, ikonknappar, statusfeedback, modaler, reduced-motion-policyn) dokumenterade i nya [accessibility.md](docs/dev/accessibility.md), inlänkad från [theming.md](docs/dev/theming.md) och CLAUDE.md; A1/A2/A3-primitiverna (IconButton/Modal/Alert/toast-live-region) markerade som planerade.
- **Delade knapp-primitiver `<Button>`, `<IconButton>` och `<Kbd>` (`frontend/src/renderer/components/shared/`).** Första byggstenen i det egna komponentbiblioteket. `<Button variant="primary|secondary|ghost|danger" size="sm|md" loading disabled type>` renderar klasskontraktet `btn btn--{variant} btn--{size}`; `loading` medför `disabled` + `aria-busy` + en inline-spinner vars animation ligger i en CSS-klass (så den kommande globala `prefers-reduced-motion`-brytaren i A5 — och den lokala guarden i `shared.css` — kan stänga av den). `<IconButton icon label variant="elevated|ghost|danger" size>` återanvänder befintliga `Icon.jsx` — default-varianten `elevated` speglar legacy `.btn-icon` exakt (upphöjd chip: vilande `--bg-elevated`, hover `--btn-icon-hover-bg` + `--shadow-sm`) så migreringar blir visuellt förlustfria, `ghost` är opt-in för lågprofil — och kräver `label` (blir både `aria-label` och `title`; saknad label ytar en dev-warning eftersom projektet saknar prop-types-beroende). `<Kbd>` är en tunn wrapper kring `<kbd>` som återanvänder den befintliga retro-`.kbd`-stilen. Nya stilar i `components/shared/shared.css` är vanliga CSS-regler mot theme-tokens och visuellt identiska med de gamla `.btn-action`/`.btn-secondary`/`.btn-danger`/`.btn-icon`-kategorierna (ghost = borderless, dämpad). **De gamla `.btn-*`-klasserna är orörda och lever kvar som alias tills städ-PR:en (B7).** Stilbladet skeppas app-brett via en side-effect-import i workspace-entryn (efter `theme.css`) och importeras dessutom av primitiverna själva (esbuild dedupe:ar). Exportyta: `components/shared/index.js`. Vitest-tester för alla tre (`frontend/tests/shared/`, 21 tester). Den semantiska modellen och migreringstabellen (btn-primary/confirm→primary, btn-cancel→secondary, btn-secondary→secondary|ghost, btn-action→primary|secondary med **max en primary per vy**, btn-danger→danger, btn-icon→IconButton) är dokumenterad i [theming.md](docs/dev/theming.md). Ingen modulmigrering i denna PR — bara primitiver.
- **Tailwind v4-grund (utility-lager ovanpå token-systemet, noll visuell förändring).** Ny `frontend/src/renderer/tailwind.css` exponerar Tailwind-utilities som mappar **1:1** mot de befintliga `theme.css`-tokenen via `@theme inline` (`bg-bg-primary`, `text-text-primary`, `border-border-medium`, semantiska `text-success`/`bg-success-bg`, `p-md`/`gap-lg`, `text-base`/`text-2xl`, `font-sans`, `rounded-lg`/`shadow-md`; enda namnavvikelse är accent-kortformen `accent`→`--accent-primary`). Eftersom utilities pekar rakt på de levande `var(--…)`-variablerna fungerar båda temana, tri-state-växlingen och ThemeEditors runtime-overrides genom lagret per konstruktion. `theme.css` förblir enda källa till sanning. **Preflight importeras inte** (projektet äger sina egna resets), och **ingen base-border-reset** läggs heller in — omigrerade formulärkontroller (`<select>`/`<input>` i TrashPanel, LogViewer, StatisticsDashboard, RenameNefModule) förlitar sig på UA-default-borders som en author-reset skulle nollställa; border-utilities kräver därför explicit border-style (`border border-solid border-border-medium`) tills kontrollerna migrerats till delade primitiver (fas A/B). Tailwind-CLI:n spawnas från `scripts/build-workspace.js` parallellt med esbuild (build: engångskörning med `--minify` i produktion; watch: `--watch=always` parallellt med esbuild-watchen, dödas rent vid process-exit) → `workspace/dist/tailwind-bundle.css`, länkad **efter** `workspace-bundle.css` i `workspace-flex.html` så olagrad legacy-CSS alltid vinner över Tailwinds `@layer`-utilities. Inga npm-script- eller CSP-ändringar. Samexistensregler, namnschema och den avstängda preflighten är dokumenterade i [theming.md](docs/dev/theming.md); kända klasskrockar (`.text-success/-warning/-error/-info`) är loggade i [ROADMAP.md](ROADMAP.md).
- **Karaktäriserings-/fence-test för `FileQueueModule` (`frontend/tests/fileQueueModule.test.jsx`, 30 tester).** Fäster nuvarande observerbara beteende hos repots största gud-komponent (~2700 rader, ~129 hooks) inför den planerade uppdelningen (H8/H9 — rena extraktioner → förbehandlings-hook → kö-reducer + rename-logik). Bygger på den befintliga rena hjälpartäckningen (`fileQueueEligibility.test.js`), som inte dupliceras. Täcker: (1) **preferens-läsare** — de åtta `localStorage`-läsarna och `naturalSortCompare` pinnas som rena enheter (defaults, endast-satta-nycklar, trasig JSON → fallback); (2) **kö-grunder** (fullmonterat) — filer från `queue-files`-IPC:t landar natur-sorterade (inkl. `IMG_2` före `IMG_10`), dubbletter ignoreras med "redan i kön"-toast, XMP-sidofiler filtreras bort, ×-knappen tar bort filen och meddelar förbehandlings-managern, och `queue-status` emittas med rätt total (plus svar på `request-queue-status`-probe); (3) **förbehandlings-orkestrering** (mockad manager) — behöriga färska filer köas, `status-change`/`completed`/`error`-event ytar rätt rad-indikator (spinner/blixt/fel) och `error`/`paused` toastar (paus-toast på som default, resume tyst); (4) **rename-flödet** — en redan-behandlad fil ytar rename-kontrollen, bekräftelse-gaten (default på) avbryter vid nej och postar rename vid ja med path-uppdatering av raden, och preferensen `requireConfirmation: false` renamer utan dialog; (5) **`FileQueueItem`-rendering** — rätt ikon + statustext per läge (pending/completed/error/missing/redan-behandlad) via en återställd kö, samt reprocess-knappen. Backend-API, modul-eventbussen, toasts, fil-watch-IPC:t, `apiClient`-singletonen och förbehandlings-managern mockas; `PreprocessingStatus`, i18n-katalogen och `Icon` lämnas äkta. Dokumenterade luckor: `loadFile`-handskakningen (openModule → waitForListeners → `load-image`), all-klar-completion-toasten (känd objekt-vs-sträng-quirk för H8) och `FileQueueItem`:s bredd-mätta namnrad (canvas `measureText` saknas under jsdom) drivs inte här. De åtta preferens-läsarna och `naturalSortCompare` exporteras nu (beteendeneutral test-söm) från `FileQueueModule.jsx`.
- **Utökat karaktäriserings-/fence-test för `CullingModule` (`frontend/tests/cullingModuleFence.test.jsx`, 19 tester).** Fäster nuvarande observerbara beteende hos gallringsvyn inför den planerade uppdelningen (H6), och **stänger** två tekniska skuld-punkter (#106, #114). Fullmonterar modulen (backend-API, modul-eventbussen, mapp-watch-IPC och singleton-thumbnail-cachen mockas; i18n-strängarna lämnas äkta) och driver den via `culling-load`-eventet. Täcker: (1) **filter-/query-flödet** — en `culling-load` fyller listan, väljer första filen och postar `culling/files`; byte av filtyp, val av spelare (`player` + `*namn*`-glob) och glob+Enter kör om queryn med rätt params; (2) **vyläges-växling** — Rutnät-knappen växlar loupe→grid och persisterar valet (`localStorage`), det persisterade grid-läget återställs vid montering, Esc i loupe → grid, Enter i grid öppnar cellen i loupe; (3) **gallra/ångra-sekvensering** — `x`/`Delete` flyttar aktuell fil till papperskorgen exakt en gång med rätt sökväg och tar bort den ur listan, `⌘Z` återställer senast gallrade id; (4) **#106 — kontextmeny-gate för Enter/Esc** — medan menyn är öppen är Enter en äkta no-op (ingen inline-rename) och läcker inte till en senare registrerad capture-fas-lyssnare (`stopImmediatePropagation`), och Esc stänger menyn utan att läcka; plus aktiv-tabset-gaten (Enter startar rename bara när gallring är aktiv tabset); (5) **#114 — `gridThumbnailCache.clear()`-inkopplingen** — cachen töms både vid unmount-teardown och vid en naken `--clear`; (6) **stats-panelen** — en rad per räknad spelare renderas, och klick på en rad i loupe filtrerar listan till spelaren. Dokumenterade luckor: grid-lägets debounce:ade single-click-highlight och preview-upplösningen (stat-file-stable/NEF-konvertering) drivs inte här. Inga produktionsändringar — rent test-tillägg.
- **Karaktäriserings- och smoke-tester för `ReviewModule` (`frontend/tests/reviewModule.test.jsx`, 18 tester).** Fäster nuvarande observerbara beteende hos granskningsvyn inför den planerade uppdelningen (H4). Täcker: (1) **smoke** — komponenten monterar utan att kasta, en faceslista renderar från ett mockat detect-resultat, och läget "inga ansikten" visar den manuella namn-affordansen ("Lägg till namn manuellt"), medan ett detektions*fel* (tomma faces men inte `detectionOk`) inte gör det; (2) **tangentnavigering** — via document-nivåns `keydown`-lyssnare: Enter bekräftar aktuellt ansikte via topp-alternativet, siffertangent väljer motsvarande alternativ, `i` ignorerar, `ArrowDown` stegar aktivt ansikte och broadcastar `active-face-changed`, Escape med osparade ändringar triggar discard-bekräftelse; gating på `node.isVisible()` (som det fungerar idag — active-tabset-uppgraderingen kommer i I1) och guard-testet att skrivning i namnfältet **inte** triggar enkeltangent-genvägar; (3) **autocomplete** — prefix-träffar rankas före substräng-träffar, skiftlägesokänsligt, tom sökning ger inga förslag; (4) **tillståndsövergångar** — bekräfta auto-avancerar till nästa ansikte, dubbelklick av-bekräftar (fältet återkommer), `review-dirty` emittas false efter detect och true när ett ansikte blir pending, och all-klar-vägen auto-sparar (`batch-confirm`) och emittar `review-complete`. Backend-API, modul-eventbussen, toasts, websocket, thumbnails och preferences mockas; i18n-katalogen, `normalizeSuffix` och `Icon` lämnas äkta så assertions kan utgå från de faktiska svenska strängarna. Dokumenterade luckor (ConfirmDialog-grenarna för namn-mismatch/hög-konfidens-ignorering samt `useDropdownPosition`-geometri under jsdom) lämnas till extraktions-PR:en.
- **Karaktäriserings- och smoke-tester för `FlexLayoutWorkspace` (`frontend/tests/flexLayoutWorkspace.test.jsx`, 20 tester).** Fäster nuvarande beteende hos arbetsytans layout-host inför den planerade uppdelningen (menu-command-switchen, `shortcutSections.js`, `applyUIPreferences`, `MODULE_COMPONENTS`-registret). Täcker: (1) **smoke** — komponenten monterar default review-layouten utan att kasta, och en känd modulflik renderar sin (mockade) komponent via `factory`; (2) **menu-command-dispatch** — driver `menu-command`-IPC-ingången med ett representativt urval kommandon (`open-culling`/`open-trash` öppnar modulflikar, `layout-comparison`/`layout-database`/`reset-layout` byter preset, `theme-light|dark|system` går till `themeManager.setPreference`, `open-file` öppnar fildialogen, okänt kommando broadcastas via `moduleAPI.emit`) och asserterar de observerbara effekterna; (3) **`applyUIPreferences`** — jsdom-test som spikar CSS-var (`--font-size`) och den injicerade `<style>`-regeln (tab-höjd/padding/gap/min-height) samt no-op-vägen utan `.flexlayout__layout`-element; (4) **`SHORTCUT_SECTIONS` data-sanity** — form, unika sektions-id, inga dubbletter av tangentkombination eller beskrivning per sektion. Alla modulkomponenter, `themeManager` och `moduleAPI` mockas; jsdom får stubbar för `localStorage`/`matchMedia`/`ResizeObserver` och en fast `getBoundingClientRect` så FlexLayout faktiskt renderar flikinnehåll. `applyUIPreferences` och `SHORTCUT_SECTIONS` exporteras nu (beteendeneutral test-söm) från `FlexLayoutWorkspace.jsx`.

### Changed
- **Svenskt skrivpass över hela strängkatalogen (fas C2).** Redaktionell genomgång av `frontend/src/i18n/sv/` — endast ordalydelse, inga komponent- eller logikändringar; interpolationer och pluralformer orörda. **Interpunktion:** typografisk ellips `…` (U+2026) i alla pågår-lägen i stället för `...`; svenska citattecken `”…”` runt namn i bekräftelsedialoger (ersätter tre blandade stilar `'…'`/`\"…\"`/`”…”`); onödiga utropstecken borttagna; "Obs:"-utfyllnad struken. **Vokabulär enhetligad genom flödena:** körningens ansiktssökning talar nu `söka`/`hitta`-familjen ("Söker ansikten…", "Inga ansikten hittades", "Hittade:") i stället för det missvisande `identifiera` (som betyder igenkänning — ett annat steg); den tekniska funktionstermen `ansiktsdetektering` behålls i inställningar/statistik. Namnbyte använder `byt namn`-familjen hela vägen (RenameNef-toasten nu "Bytte namn på …", i linje med Filkö). Förhandsvisning → `förhandsgranskning`; `foton` → `bilder`. **Fel i aktiv form med nästa steg:** operationsfel normaliserade till "Kunde inte …" (matchar knappen som utlöste dem) i stället för passivt "… misslyckades"; terse fel fick ett konkret nästa steg ("Filen hittades inte. Den kan ha flyttats eller tagits bort."). **Tomlägen som inbjudningar:** Cullings lupp- och tomresultatlägen bjuder in nästa åtgärd i stället för att konstatera ett faktum. Ordlista + principer dokumenterade i [contributing.md § UI Copy](docs/dev/contributing.md); [workspace-guide.md](docs/user/workspace-guide.md) uppdaterad till samma vokabulär. Tre fence-tester fick sina förväntade strängar synkade med katalogen; 473 tester gröna.
- **`DatabaseManagement` och `RefineFacesModule` migrerade till de delade primitiverna (fas B1).** De två modulerna byter från handrullat UI till fas A-verktygslådan. **Native-dialoger borta:** de tio `window.confirm()`-anropen (Database: byt namn, slå samman, slå samman par, dedup, radera, flytta till ignorerade, flytta från ignorerade, ångra, rensa; RefineFaces: tillämpa filtrering) ersätts av promise-baserade `useConfirm()` — bekräftelsen sker tidigt i handlern, före tillståndsändringar, och destruktiva åtgärder (radera person, rensa/dedup kodningar, tillämpa filtrering) får `variant:'danger'`. `ConfirmProvider` serialiserar samtidiga anrop, så ett dubbelklick under väntan kan inte köra åtgärden två gånger. De två `alert()`-anropen (Databas: senaste-filer-lista; RefineFaces: formreparations-detaljer vid simulering) ersätts av `<Modal>`-dialoger med den fulla listan. **Feedback enhetligad:** de transienta `.status-message`-kvittona blir toasts via `useToast()` (success/info/warning), medan persistenta fel och valideringsmeddelanden renderas genom den delade `<Alert>`-primitiven (`role="alert"`, avfärdbar) — inklusive RefineFaces förhandsgransknings-varningar. **Knapp-triage** (theming.md-modellen, max en primary per panel): varje operationskort får sin huvudåtgärd som `primary` (byt namn, slå samman, hitta, sök, flytta ×2, ångra), destruktiva åtgärder som `danger` (radera, rensa/purge, tillämpa filtrering, reparera former), och sekundära/rad-åtgärder som `secondary`/`ghost` (ladda om, behåll, rensa, inte-en-dubblett, simulera, visa senaste filer); ikon-endast "ta bort uteslutning" blir `<IconButton>` med svensk `aria-label`. **Autocomplete:** de `<datalist>`-baserade namnfälten ersätts av den delade `<Autocomplete>`-comboboxen (`selectOnEnter`, ranking via den befintliga namnlistan; purge-fältet inkluderar `ignore`). **Enter-to-submit:** varje operation blir ett riktigt `<form onSubmit>` så Enter i valfritt fält kör åtgärden. i18n: nya nycklar (`database.dialogs.recentFilesTitle`, `database.emptyStates.noRecentFiles`, `refineFaces.messages.repairModalTitle`) i rätt namespace; borttagna oanvända nycklar (`database.dialogs.recentFiles`, `refineFaces.messages.repairAlert`). Ersatt legacy-CSS (`.db-excluded-remove`, `.warning-message`) borttagen ur modul-CSS:erna; `.cell-keep`/`.cell-remove`:s fantomtokens (`--status-success`/`--status-error`, definierade ingenstans — färgkodningen var död i båda teman, samma buggklass som #165) bytta till `--color-success-text`/`--color-error-text`; de delade `.btn-*`/`.status-message`-klasserna i `theme.css` lämnas orörda (används fortfarande av omigrerade moduler). FormField-primitiven byggdes **inte** — bägge modulernas fält är placeholder-drivna inline-rader eller täta grid-layouter där en staplad label+hint+error-primitiv hade krävt omdesign; Enter-to-submit levereras i stället via `<form>`. Inga tester behövde ändras (473 gröna); ingen visuell omdesign.
- **Inline-CSS flyttad ur `workspace-flex.html` till CSS-bundlen (ren refaktor, noll visuell förändring).** FlexLayout-tema-bryggan, de strukturella `flexlayout__*`-overridesen och de globala toast-stilarna flyttades verbatim till nya `frontend/src/renderer/workspace/flexlayout/flexlayout-overrides.css`, som importeras **sist** i `workspace/flexlayout/index.jsx` (efter `theme.css` och `flexlayout-react/style/light.css`) så kaskadläget bevaras — tidigare vann inline-blocket via dokumentordning, nu via importordning. Reset-/`html,body`-/`#workspace-root`-blocket konsoliderades in i `theme.css`:s befintliga `BASE RESETS`-sektion (den dubblerade `*`-regeln slogs ihop till en). Kvar i HTML-huvudet är bara `<link>` till bundeln plus CSP-metataggen (orörd).
- **Hårdkodade färger tokeniserade (canvas + inline-stilar).** Kvarvarande hårdkodade hex/rgba som kringgick temasystemet flyttades till `theme.css`-tokens: (1) `ImageViewer`:s fyra match-konfidens-nyanser för ansiktsrutor (amber/blå/orange/röd + rgba-varianter) fick nya tokens `--face-uncertain-*`, `--face-confidence-medium-*`, `--face-confidence-low-*`, `--face-confidence-none-*` (definierade i **båda** temablocken, lika värden — rutorna ritas över fotot, inte över UI-kromet) och läses via samma cachade `getComputedStyle`-mönster som de befintliga `--face-confirmed/-ignored`-färgerna (invalideras vid temabyte); (2) `OriginalView`:s canvas-clear (`#2a2a2a`) läser nu `--bg-elevated` och repaintar vid temabyte; (3) `FlexLayoutWorkspace`:s inline `color:'#666'` (okänd-modul + laddnings-platshållare) bytt mot ny klass `.workspace-placeholder-text` som använder `var(--text-tertiary)`; (4) `FileQueueModule.css` `font-family: monospace` → `var(--font-mono)`. Inga visuella ändringar i normalfallet — de nya tokens behåller dagens färger.
- **`FileQueueModule.jsx` — hooks och kö-reducer (refaktorering H9, steg 3 av 3, beteendeneutral).** Sista uppdelningssteget av gud-komponenten, i två logiska etapper under `components/fileQueue/`. **Etapp A — förbehandlings-orkestrering:** `usePreprocessing.js` äger nu hela `getPreprocessingManager`-bindningen (singleton-ref, alla `manager.on(...)`-prenumerationer `status-change`/`completed`/`error`/`file-not-found`/`paused`/`resumed`/`cache-cleared`/`already-processed`, paus/resume-toasts, missing-fil-batchning, hash-checkern, backend-cache-prioriteringen och "allt klart"-toasten). Ren flytt; skalet konsumerar det returnerade förbehandlingstillståndet, status-refen, paus-flaggan och `countPreprocessed`. En separat rättning: "allt klart"-toasten jämförde `preprocessingStatus`-objekten mot sträng-enumet `PreprocessingStatus` (matchade aldrig, toasten avfyrades aldrig) — jämför nu `.status`-fältet (den kända egenheten som fence #157 dokumenterade). **Etapp B — kötillstånd + rename:** `queueReducer.js` är en ren reducer över `{ queue, currentIndex }` med namngivna actions (`restore`, `add`, `sort`, `removeById`, `removePaths`, `clear`, `clearDone`, `clearSelected`, `setActive`, `clearProcessedFlag`, `setReviewed`, `markProcessed`, `markMissing`, `markProcessedByNames`, `applyRename`, `insertAt`, `setIndex`) — enumererade ur skalets alla `setQueue`/`setCurrentIndex`-anropsställen och direkt enhetstestade (26 tester, inkl. `currentIndex`-justeringarna som fence-testet inte driver); `useFileQueue.js` wrappar den med `useReducer` + synkad `queueRef` + bundna action-dispatchers. Rename-flödet lyftes till `renameLogic.js` (rena sökvägs-/namnberäkningar: behörig-sökvägsval, preview-/renamed-map-bygge, applicering på kön, status-map-omnyckling, resultatsammanfattning — 12 enhetstester) och `useNefRename.js` (React-bindning: preview-toggle, preview-lookup, in-progress-flagga, `fetchRenamePreview`/`handlePreviewToggle`/`handleRename`). `usePreprocessing` och `useNefRename` tar de specifika bundna actions de behöver i stället för rå `setQueue`. Skalet krympte från ~2202 till ~1743 rader. Karaktäriseringstesterna (`fileQueueModule.test.jsx`, #157) står gröna med enbart omriktade importer. **Medvetna stopp:** kö-status-emiten och `localStorage`-restore/persist-effekterna lämnades i skalet — de bryggar systertillstånd (förbehandlingsstatus, rename-toggle, auto-advance/fix-mode) och kan inte flyttas utan att återinföra den kopplingen. **`RenameNefModule` (EXIF/CreateDate-rename av rå-filer via `/rename-nef/*`) är ett separat flöde** utan delad kod eller endpoints med detta ansiktsnamns-suffix-rename (`/files/rename*`) — ingen sammanslagning gjord.
- **"Öppna i Lightroom" cachar RAW-rotens filnamnsindex mellan tangenttryck (prestanda).** `open-raw-in-lightroom`-IPC-hanteraren löste tidigare en visad JPEG till sin käll-NEF genom att läsa *hela* RAW-roten rekursivt i minnet vid *varje* anrop — en full `readdir(recursive)`-genomgång per tangenttryck. Räckte för dagens per-match-mappar men var patologiskt för en stor RAW-rot. Genomgången är nu lyft till nya `frontend/src/main/raw-index.js` (`createRawIndexCache`): roten skannas **en gång**, byggs till en `token → sorterade sökvägar`-karta (nyckel = det ledande `YYMMDD_HHMMSS[-N]`-tidsstämpeltoken från `raw-match.js`) och återanvänds över efterföljande uppslag. **Invalidering är TTL-baserad** (30 s): en tangentbords-burst träffar den varma cachen medan en nyimporterad RAW dyker upp inom ett TTL-fönster. En filsystem-watcher på RAW-roten hade varit tightare men ingen befintlig watcher täcker den (`watch-folder`-watcherna är knutna till arbets-/kömappar, inte RAW-roten), och en dedikerad watcher hör hemma i den planerade chokidar-konsolideringen, inte här. **Matchningssemantiken är oförändrad:** indexet inkluderar exakt de filer `basenameMatchesToken` skulle ha matchat (RAW-extension + exakt token-likhet), och varje tokens sökvägar sorteras lexikografiskt precis som den gamla inline-`matches.sort()`, så `lookup` returnerar samma deterministiska första-träff som förr. Nya enhetstester (`frontend/tests/rawIndex.test.js`): bygg (endast RAW med token indexeras, JPEG/XMP/tokenlösa/kataloger hoppas över), uppslag (deterministisk sorterad första-träff, exakt token-vs-burst-särskiljning, skiftlägesokänslig extension, okänt/falsy token → null), TTL-utgång (burst inom fönstret = en skanning, korsad TTL-gräns = exakt en till) med injicerad klocka, per-rot-cachning och `invalidate()`.
- **`FileQueueModule.jsx` — rena extraktioner (refaktorering H8, steg 1 av 3, beteendeneutral).** Repots största gud-komponent (~2700 rader) började brytas upp med de lägst-riskabla, rena flyttarna, under `components/fileQueue/`: `fileQueuePrefs.js` (de åtta `localStorage`-preferensläsarna `getAutoLoadPreference`, `getRenameConfig`, `getNotificationPreference`, `getPreprocessingConfig`, `getRequireRenameConfirmation`, `getAutoRemoveMissingPreference`, `getToastDurationMultiplier` och `getInsertModePreference` — oförändrade kroppar, fortsatt utan preferens-modulimport för att undvika cirkulärt beroende), `queueUtils.js` (de rena hjälparna `naturalSortCompare`, `generateId` och konstanten `SUPPORTED_EXTENSIONS` — flyttad verbatim; den kända frågan i ROADMAP om att RAW-mängden `nef/cr2/arw` är snävare än backendens nio format lämnas medvetet orörd) och `FileQueueItem.jsx` (rad-subkomponenten ~330 rader — ren flytt med oförändrad kropp och prop-form; dess egna importer omriktade till den nya djupare sökvägen, och den nu oanvända `nameFormatter`-importen togs bort ur skalet). Skalet krympte från ~2703 till ~2202 rader. Karaktäriseringstesterna (`fileQueueModule.test.jsx`, #157) står gröna med enbart omriktade importer: de åtta preferensläsarna importeras nu från `fileQueue/fileQueuePrefs.js` och `naturalSortCompare` från `fileQueue/queueUtils.js` — assertions oförändrade. Avsiktligt kvarlämnat till H9: förbehandlings-hooken, kö-reducern och rename-logiken.
- **`CullingModule.jsx` uppdelad i fokuserade moduler (refaktorering H6, beteendeneutral).** Gallringsvyn (~1700 rader) blandade UI-skal, subkomponenter, ren query-/sökvägslogik, persistens och preview-maskineri; den krympte till ~1290 rader genom att lyfta ut, under `components/culling/`: `cullingQueryUtils.js` (de rena hjälparna `statsScopeFromQuery`, `isRaw`, `globBaseDir`, `basename`, `stripExt` — oförändrade kroppar, med egna enhetstester), `StatsPanel.jsx` (`CullingStats` + `CullingExcluded` + `EXCLUDED_LABELS`, ren flytt), `useCullingPreview.js` (loupe-previewens tillstånd url/loading/error, mapp-noncen och de tre effekterna JPEG-stat-settle, RAW-konvertering och RAW-prefetch — verbatim; skalet konsumerar det returnerade tillståndet och anropar `bumpPreview()` från mapp-watch-handlern) samt `FilterBar.jsx` och `ContextMenu.jsx` (filter-/åtgärdsbaren respektive högerklicksmenyn som presentationskomponenter med smala props). Dessutom ny generisk, återanvändbar hook `hooks/usePersistedValue.js` (en `useState`-variant som speglar värdet till `localStorage` med valfria `parse`/`serialize`); `CullingModule`s `statsWidth`, `listPct` och `viewMode` (tidigare `readStoredNumber`/`readStoredString` + tre skriv-effekter) migrerades till den. Medvetet kvarlämnat i skalet (samma bedömning som H2/H4): query-tillståndsklustret och `loadList`/`runFilter`/`loadStats`-orkestreringen, modul-eventen (`cull-player`, `culling-load`, adopt-effekten), gallra/ångra-loopen och särskilt **capture-fas-tangentgaten** för Enter/Esc — dess ordning (`stopImmediatePropagation` *före* `menuRef`-bailen) är fäst av fence-testet #106 och är för känslig för att flyttas utan risk. Alla 61 culling-tester (inkl. 19-testers-fencet #155) gröna utan omriktning; nya enhetstester för `cullingQueryUtils` och `usePersistedValue`.
- **Half-size RAW-decode för detektion vid cachemiss (prestanda).** När en RAW-fil detekteras utan att preprocessing-cachens JPG finns (t.ex. direktöppning av NEF utan förbearbetning) gjorde `DetectionService` en full rawpy-demosaic (~1,75 s för en 45 MP NEF). Detektionsvägen använder nu `raw.postprocess(half_size=True)` (~0,65 s, ~2,7× snabbare) via nya `_load_image_for_detection`, som returnerar `(rgb, coord_scale)`. **Koordinatkedjan är intakt:** frontend visar den fullupplösta förbehandlade JPG:en och ritar rutor i dess pixelrymd, så `coord_scale` (härledd exakt ur `raw.sizes`, ej antagen 2,0) viks in i `_detect_and_match_faces`' befintliga skalfaktor — API-resultat, encoding-cachen och DB-skrivningar får därmed fortsatt fullupplösta koordinater. **Kvalitet verifierad på riktiga NEF:ar:** samma antal ansikten, IoU 0,976–0,993 och embedding-cosinuslikhet 0,966–0,994 mellan full- och half-decode (detektionen nedskalar ändå till ≤4500 px, så half-decode på 4140 px är i praktiken förlustfri för detektorn). Cachad-JPG-vägen och standardformat är oförändrade (scale 1,0); visnings-/miniatyr-vägarna (`_load_image`, `convert_nef_to_jpg`, `get_face_thumbnail`) behåller full upplösning. `detection_meta.original_size` rapporterar fullupplösningsdimensioner; `scale_label` blir `"half"` när half-decode användes utan vidare nedskalning. Två nya tester pinnar koordinatskalningen (bbox exakt dubblad vid `coord_scale=2.0`; standardformat ger scale 1,0).
- **Engångs-normalisering av DB:n med schema-markör i `core.db.load_database` (prestanda, Sprint 2).** `load_database` körde tidigare `normalize_encoding_entry` över *varje* post vid *varje* laddning (legacy-migrering: naken ndarray→dict, saknad `backend`→`dlib`, backfill av `encoding_hash`/`backend_version`/`created_at`). Med store:t som laddar en gång per process spelade det mindre roll än förr, men CLI:t laddar fortfarande per körning och varje reload betalade hela per-post-passet. Nu: första laddningen av en o-migrerad DB normaliserar, **sparar tillbaka** det normaliserade resultatet direkt (via `save_database(only=…)` — bara de kollektioner som faktiskt hade migrerade poster) och skriver en `db_meta.json`-sidecar (`{"schema": 2}`) atomiskt (temp-fil + rename); efterföljande laddningar läser markören och **hoppar över** per-post-passet helt. Ny konstant `DB_SCHEMA_VERSION = 2` (bumpas om `normalize_encoding_entry`:s utdataform ändras — en äldre markör tvingar då fram nytt fullt pass + re-spar). **Säkerhet (rör oersättlig användardata):** (1) datafilerna skrivs om **bara** när normaliseringen faktiskt ändrade något (`total_migrated > 0`) — en no-op-laddning skriver aldrig om picklarna, bara den lilla markören; (2) om **någon** post är korrupt (varken array eller dict) görs *ingenting* — ingen save-back (som tyst hade droppat den korrupta posten från disk, till skillnad från idag) och ingen markör (så DB:n fortsätter re-normaliseras varje laddning, exakt som förr); (3) saknad/trasig/äldre markör faller tillbaka till fullt pass. **Att lita på markören är säkert** eftersom varje konsumtionsställe redan tolererar nakna arrayer och backend-lösa dictar och defaultar till `dlib`-backend (`core.matching.filter_database_by_backend`/`best_matches`, samt management-/refinement-/statistics-tjänsterna) — en legacy-post som smiter förbi markören via ett externt verktyg ignoreras därför ofarligt i matchningen i stället för att krascha den (belt-and-braces). Ny exakt migration-predikat-hjälpare `_entry_needs_normalization` (räknar även enbart-`encoding_hash`-backfill, som det gamla räknesättet missade) och `_normalize_collection` (returnerar migrated/corrupt-antal). Mätning på syntetisk 5000-posters DB (500 personer): redan-migrerad laddning föll ~32 % (≈8,0 → ≈5,4 ms/laddning) — per-post-passet är borta; kvarvarande kostnad är pickle-deserialiseringen. Tester: nio nya i `test_faceid_db.py` (första laddningen migrerar + sparar tillbaka + skriver markör; andra laddningen spionerar på `normalize_encoding_entry` och bevisar noll anrop; markör saknas/trasig/äldre → fullt pass + markör återskapas; redan-ren-utan-markör skriver markör men skriver *inte* om datafilen; korrupt post → ingen markör, ingen save, korrupt post kvar på disk; markör-present-men-legacy-post → ingen krasch, rå post returneras; predikat-enhetstest). Alla 32 befintliga faceid_db-tester gröna (`db_dir`-fixturen redirectar nu även `DB_META_PATH`).
- **`ReviewModule.jsx` uppdelad i fokuserade moduler (refaktorering H4, beteendeneutral).** Granskningsmodulen (~1540 rader) blandade UI-skal, subkomponenter, autocomplete-rankning, tangentbordsrouting och tillståndsövergångar; den krympte till ett orkestrerings-skal (~920 rader) genom att lyfta ut, under `components/review/`: `SuffixDialog.jsx`, `ConfirmDialog.jsx` och `FaceCard.jsx` (rena flyttar, oförändrade kroppar och prop-former), `nameAutocomplete.js` (den inline-rankningen som ren `rank(query, names, limit=8)` — prefix-träffar före substräng, skiftlägesokänsligt), `reviewActions.js` (rena tillståndsövergångar över faceslistan: `getTopMatch` med 75 %-tröskeln, `confirmFaceState`/`ignoreFaceState`/`unconfirmFaceState`/`undoFaceState`/`acceptAllState`, navigationens `nextFaceIndex`/`willAllBeDone`, pending-listornas `upsertConfirmation`/`appendIgnore` respektive `mergeConfirmations`/`mergeIgnores` — hållna som separata hjälpare eftersom accept-all, till skillnad från enkel-confirm, även ersätter `suggested_name` på befintliga poster — samt `buildReviewedFaces`), och `useReviewKeyboard.js` (dokumentnivå-keydown-lyssnaren som hook: äger lyssnarlivscykeln, input-/blockerande-modul-guards och tangent→handler-routing inkl. preventDefault; modulen levererar grenkropparna som callbacks, och gaten är en parameter `isActive: () => !node || node.isVisible()` så I1 kan byta källa till aktiv-tabset utan att röra routingen). `useDropdownPosition` flyttade till `hooks/useDropdownPosition.js` (delad hook-konvention). Orkestreringen — setState-trådning, event-emits, dialoggating, fokus-timeouts och API-/autosparande-sekvensering — lämnades avsiktligt kvar i skalet (samma bedömning som H2:s accepterade utfall). Alla 18 karaktäriseringstester (#151) gröna utan omriktning. Nya enhetstester (30 st) stänger de dokumenterade luckorna: `confirmDialog.test.jsx` (name-mismatch- och hög-konfidens-ignoreringsgrenarna som direkta komponenttester mot de riktiga svenska strängarna), `reviewActions.test.js` (per-fält-pinning av confirm-/ignore-posterna som utgör batch-confirm-payloaden, inkl. trimning och `suggested_name`-reglerna) och `nameAutocomplete.test.js` (rankningsreglerna direkt).
- **Per-kollektion dirty-flag-sparningar i `core.db` och `FaceDBStore` (prestanda, Sprint 2).** `core.db.save_database` tog tidigare alltid om *alla fyra* filerna (`encodings.pkl`, `ignored.pkl`, `hardneg.pkl`, `processed_files.jsonl`) vid varje spar — auditens största write-amplification-post. Funktionen tar nu en valfri `only`-parameter (delmängd av `{'known','ignored','hardneg','processed'}`; `None` = alla fyra, bevarar CLI-/legacy-beteende, okända namn ger `ValueError`); bara de namngivna filerna skrivs om (atomisk rename + `flock` per fil oförändrat), och en enfils-spar hoppar över trådpoolen. `FaceDBStore.mutate` fick ett explicit `touches={...}`-argument som deklarerar vilka kollektioner muteringen skriver (`None` = konservativt alla fyra). Dirty-flaggor ackumuleras nu **per kollektion** över koalescerade muteringar; den debounce:ade sparningen skriver exakt den dirty-unionen via `save_database(only=...)`, nollställer sedan flaggorna och om-registrerar fingeravtryck **bara** för de skrivna filerna (de orörda filernas registrerade fingeravtryck matchar fortfarande disken, så extern-ändringsdetektion för dem är intakt). Snapshot+nollställning av dirty-setet och sparningen sker helt under store-låset, och `mutate` tar samma lås — så ingen mutering kan landa mellan snapshot och nollställning (ingen förlorad skrivning); flaggorna nollställs först efter lyckad spar. Nettoeffekt på review-hot-pathen: en confirm-identity skriver nu om `encodings.pkl` (+ `hardneg.pkl` bara när användaren rättat ett förslag) och — separat — `processed_files.jsonl`, **aldrig** `ignored.pkl`; en ignore skriver bara `ignored.pkl`. De tre migrerade tjänsternas muteringar (management: rename/merge/delete/ignore/undo/purge/dedup; detection: confirm/ignore/processed; refinement: remove-dlib/outliers/repair; rename_service: sökvägsuppdatering) deklarerar nu exakta `touches`-mängder. Tester: `test_faceid_db.py` fick `only`-parametertester (bara namngivna filer skrivs om — mtime på övriga oförändrad; delmängd; tom mängd = no-op; okänd = `ValueError`); `test_db_store.py` fick dirty-union-koalescering (touches known + processed → en spar skriver exakt dessa två, aldrig ignored), flaggor nollställda efter spar, partiell fingeravtrycksregistrering (extern ändring av orörd fil upptäcks fortfarande), confirm-liknande mutering rör inte ignored/hardneg, samt flush med partiell dirty. Publikt tjänste-API och rutter oförändrade.
- **`FlexLayoutWorkspace.jsx` uppdelad i fokuserade moduler (refaktorering, beteendeneutral).** Layout-hosten (~1430 rader) blandade fyra jobb och krympte till ett skal (~600 rader) genom att lyfta ut, under `workspace/flexlayout/`: `shortcutSections.js` (`SHORTCUT_SECTIONS`-katalogen), `ShortcutsHelp.jsx` (kortkommando-overlayen som subkomponent, med sin CSS-import), `moduleRegistry.js` (`MODULE_COMPONENTS` + `MODULE_TITLES`), `uiPreferences.js` (`applyUIPreferences` + ny `useUIPreferences`-hook), `layoutGeometry.js` (tabset-geometri och panel-flytt/-byte/-gruppering som rena funktioner som tar `(model, layoutRef)` — skalet wrappar dem i `useCallback`), och `menuCommands.js` (den ~40-fallens menu-command-`switch` konverterad till en dispatch-tabell `{ commandId: handler }` + `createMenuCommandHandler`, med default-fallbacken `moduleAPI.emit(command, {})` bevarad för okända kommandon). Flyttade kroppar är oförändrade förutom mekanisk ctx-trådning. `SHORTCUT_SECTIONS` och `applyUIPreferences` re-exporteras från `FlexLayoutWorkspace.jsx` så karaktäriseringstesterna (#146, 20 tester) står gröna utan omriktning. Nya enhetstester för geometri-hjälparna (`frontend/tests/layoutGeometry.test.js`, 10 tester) täcker luckan #146 dokumenterade.
- **Versionsinvaliderat matchnings-index för detektion (Sprint 3, prestanda).** `DetectionService`:s fyra matchningshjälpare (`_match_encoding`, `_match_ignored`, `_match_encoding_alternatives`, `_person_match_encodings`) byggde tidigare om per-backend-kandidatmatriserna från store:ts kollektioner vid *varje* anrop — en färsk `np.array`/`np.vstack`-genomgång över alla personers encodings för *varje* detekterat ansikte. Med ~100+ personer dominerade den upprepade O(n)-matriskonstruktionen matchningskostnaden. Nya `api/services/matching_index.py` (`MatchingIndex`) bygger de stackade matriserna **en gång per store-version** och återanvänder dem över varje ansikte i en bild (och över bilder) tills DB:t faktiskt ändras. Tre strukturer hålls för den aktiva backenden, var och en en trogen förkompilering av vad en hjälpare byggde inline: `known_lenient` (`{namn: matris}`, dlib-default-filtret från gamla `_known_candidates`, matar nearest-person + twin-k-NN-unionen), `known_strict` (`[(namn, matris)]`, det striktare explicit-backend-nyckel-filtret för alternativ-rankningen) och `ignored` (en stackad matris i oförändrad ordning så `_match_ignored`:s argmin-index är identiskt). **Invalidering** är knuten till `FaceDBStore.version`: store:t bumpar versionen vid varje mutering och vid varje extern omladdning, så ett inaktuellt index kan aldrig överleva en confirm/ignore eller en bakgrundsomladdning. **Trådsäkerhet:** matchning körs i executor-trådar, så ombyggnaden skyddas med double-checked-låsning (samma mönster som tjänste-singletonerna) — en versionsförflyttning triggar exakt en ombyggnad även under samtidiga detects; ombyggnaden körs inne i `store.read` (en konsistent genomgång under store-låset) och de resulterande matriserna är oföränderliga snapshots som pågående matchare kan fortsätta använda. **Resultatekvivalens verifierad:** nytt test (`tests/test_matching_index.py`) reimplementerar de fyra hjälparna exakt som vid fork-punkten (naiv referens) och kör naiv-vs-index över en syntetisk DB (12+ personer, flera encodings/person, ignorerade, hardneg, off-backend- och manuella/None-poster) för många probe:ar — asserterar identiska utdata (avstånd, tie-break, ordning) för både `insightface`- och `dlib`-aktiv backend; ett invaliderings-test driver den riktiga confirm-vägen och bevisar att nästa matchning ser den nyskrivna personen; ett ombyggnads-test bevisar exakt en ombyggnad per stabil version. Mikrobenchmark (100 personer × 5 enc, dim 128, 200 ansikten, nearest+ignored+alternatives): ~1,5× snabbare (~300 ms → ~197 ms) — en konservativ undre gräns då den utesluter den per-anrops-`store.read()`/fil-stat-overhead indexet också tar bort i produktion. Publikt tjänste-API och rutter oförändrade; alla karaktäriseringstester gröna.
- **Tjänste-singletons blev lata (Fas D, steg D5) — att importera en tjänstemodul har inte längre sidoeffekter.** `DetectionService`, `StatisticsService`, `PlayerCountService`, `CullingService`, `RenameNefService` och `ImportService` konstruerades tidigare på modulnivå (`x_service = XService()`) och kördes alltså vid *import*: för `detection_service` innebar det full InsightFace-laddning, läsning av den riktiga DB:n och appends till `~/.local/share/faceid/ansikten.log` — vilket flera testfiler triggade bara genom att importera modulen. Varje sådan singleton är nu ersatt av en lat getter (`get_x_service()`, samma mönster som `management_service`) som konstruerar instansen först vid första anropet. Att importera `api.services.detection_service` gick från ~7 s (InsightFace) till ~0,2 s. Alla importörer omriktade till getterna: rutterna (`detection`, `statistics`, `culling`, `player_count`, `rename_nef`, `imports`), `server.py`:s lifespan (`eager_load_ml` + trash-purge) och `detection_service`:s interna statistik-cache-invalidering samt dess modulnivå-preprocessinghjälpare. **Startbeteende oförändrat:** `server.py`:s `eager_load_ml` anropar nu getterna i bakgrundstråden vid boot, så modellerna laddas fortfarande ivrigt (verifierat: `/api/v1/startup/status` visar `mlModels: ready` efter ~7 s, `database: ready`). **Tester rör inte längre riktiga datamappen:** `tests/conftest.py` har nu två autouse-fixtures — en som redirectar `LOGGING_PATH` till en tmp-fil (och avlägsnar ev. rot-`FileHandler` mot den riktiga loggen) så ingen test skriver `ansikten.log`, och en som nollställer de lata getter-singletonerna (inkl. `management_service`) runt varje test. **Trådsäkerhet:** alla sju getters (de sex nya + `management_service`) använder double-checked locking (modulnivå-`threading.Lock`) — första anropen kan komma samtidigt från arbetartrådar (t.ex. preprocessings `ThreadPoolExecutor` via detection-modulens hjälpare), och en oskyddad check-then-set hade kunnat konstruera flera instanser (flera InsightFace-laddningar, splittrade cachar). Nytt test (`tests/test_lazy_singletons.py`) racear 8 trådar mot varje getter med konstruktionen utbytt mot en långsam räknande fabrik och asserterar exakt en konstruktion.
- **`StatisticsService` migrerad till `FaceDBStore` + kvarvarande blockerande DB/hash-anrop lyfta av event-loopen (Fas D, steg D4).** Tjänsten laddade tidigare hela DB-pickeln på nytt per ocachead förfrågan (`load_database()` i `count_faces_per_name`, `get_file_stats` och `get_summary`) med en 30 s TTL-cache. DB-läsningarna går nu genom det delade store:t: `count_faces_per_name`, `get_file_stats`:s `name_to_hash`-mappning och `get_summary`:s (namn→antal, processed-antal) beräknas inne i `store.read(fn)` under store-låset — den per-förfrågan-pickle-laddningen är borta. **Cache-design (per datakälla):** `get_summary`:s beräknade svar cachas nu **versionsnycklat** på `store.version` *och* begränsat av `cache_ttl` som skyddsnät. DB-delarna (topplista, antal filer) invalideras **omedelbart** när store:t muteras (versionen rör sig) och är giltiga hur länge som helst medan inget ändras; TTL:n behövs bara för de delar som *inte* ligger i store:t — attempt-loggen (`attempt_stats.jsonl`) och applikationsloggen (`ansikten.log`) — som ändras på disk utan att bumpa store-versionen, så de får en tidsbegränsad staleness i stället. `get_file_stats`:s `get_file_hash` (kan hasha stora NEF:er) körs nu via `asyncio.to_thread`. Docstring-buggen "cached for 2 seconds" (faktisk TTL var 30 s) rättad. Publikt API oförändrat. Karaktäriseringstesterna avsiktligt omriktade: det gamla "load-once-per-uncached-request"-testet är nu `test_summary_caches_by_store_version` (spionerar store-läsningar + attempt-log-laddningar, bevisar versionsnyckel-invalidering); shape-testet är oförändrat.
- **Blockerande DB/hash-anrop lyfta av event-loopen i rutterna (Fas D, steg D4).** `routes/database.py`:s `get_people_names` (async) anropade `load_database()` synkront på event-loopen → ersatt av `await asyncio.to_thread(get_db_store().read, …)`. `routes/files.py`:s två `get_file_hash`-anrop (manual-suffix GET/POST, async-handlers som kan hasha stora NEF:er) körs nu via `asyncio.to_thread`. `routes/detection.py`:s `reload_database` (redan store-baserad sedan D3) och `routes/preprocessing.py` (redan `run_in_executor`) lämnades. Inventering av routes efter direkta synkrona `load_database`/`save_database`/`get_file_hash` i async-defs: bara dessa två filer återstod.
- **`RefinementService` migrerad till `FaceDBStore` (Fas D, steg D4).** Tjänsten höll egna kopior av de fyra kollektionerna (laddade i `__init__`), en 2,0 s TTL-cache (`reload_database`) och synkrona `save()`-anrop (`save_database`) — ett direkt skrivvägs-bypass som hade klubbat store:ts tillstånd. Allt det borttaget: `preview` läser genom `store.read(fn)` (tung numpy-scan under låset, som `ManagementService.find_duplicate_people`); `remove_dlib_encodings`, `apply` och `repair_shapes` planerar först under `read()` (så en no-op eller dry-run schemalägger ingen save) och muterar sedan bara genom `store.mutate(fn)` + `store.flush()` när något faktiskt tas bort. Publikt tjänste-API och refinement-rutterna oförändrade.
- **`RenameService`:s DB-skrivväg flyttad till `FaceDBStore` (Fas D, steg D4).** `_update_database_paths` (uppdaterar `file`/`name`-sökvägar i known_faces/processed_files efter filbyte) laddade och sparade DB:t direkt (`load_database`/`save_database`) — en skrivväg som hade klubbat store-tillståndet. Den planerar nu räkningen under `read()` och skriver bara genom `store.mutate` + `store.flush()` när minst en post ändras. `preview_rename`:s `load_database()` (endast läsning för `collect_persons_for_files`) ersatt av `store.read`. Inga andra tjänster rör DB-filerna direkt: `import_service`/`culling_service`/`player_count_service` går via `core.playerstats`/filoperationer, `manual_suffix_service` och `preprocessing_cache` rör egna filer resp. bara `get_file_hash` — lämnade orörda.
- **`DetectionService` migrerad till `FaceDBStore` (Fas D, steg D3).** Tjänsten höll tidigare egna kopior av de fyra DB-kollektionerna (laddade i `__init__` via `load_database`) plus sitt eget debounce-maskineri (`_schedule_save`/`_flush_save`, en `asyncio.Lock` + 500 ms `asyncio.sleep`). Allt det togs bort och ersattes av det process-globala store:t: läsningar går genom `store.read(fn)` och muteringar genom `store.mutate(fn)` (in-place-redigering av kollektionerna, `d[k]=…`/`lst.append(…)`, eftersom store:t håller referensen). **Lås-disciplin för den tunga numpy-matchningen:** matchningshjälparna (`_match_encoding`, `_match_ignored`, `_match_encoding_alternatives`, `_person_match_encodings`) *snapshotar* per-backend-kandidatmatriserna (`np.array(...)`) inne i `read()` och kör `compute_distances` **utanför** låset — store-låset hålls aldrig över bilddekodning eller InsightFace-inferens (som ligger före matchningen), och inte heller över själva distansberäkningen. **Save-kadens oförändrad:** per-ansikte-`confirm_identity`/`ignore_face` anropar bara `store.mutate` (som schemalägger store:ts leading-coalesce-debounce på 500 ms, avsiktligt identisk med den gamla) — ingen `flush()` läggs till på per-face-bekräftelser; batch- och slutför-endpoints (`batch_confirm`, `mark_review_complete` när en fil markeras klar) behåller en motsvarande `store.flush()` för omedelbar hållbarhet. **Cache-invalidering:** detektions-cachen (matchnings*förslag*, nycklad `file_hash@registry-mtime`) töms nu när `store.version` rört sig sedan den senast pinnades (en bekräftelse här, eller en extern reload som store:ts fingerprint-koll fångar) — nyckelns format är oförändrat (karaktäriseringstesterna för cache-nyckeln står gröna); `reload_database` laddar inte längre DB:t själv utan tömmer de lokala cacharna och läser färska antal genom store:t (som laddar om från disk vid extern ändring), och re-pinnar cachen till aktuell store-version. Publikt tjänste-API och rutter är oförändrade. Karaktäriseringstesterna: match-case- och cache-nyckeltesterna oförändrade och gröna; `reload_database`-testet och de två debounce-testerna avsiktligt omriktade till store:t (markerade i testfilen), och tre detektionstest-fixtures (`test_detection_service`, `test_twin_disambiguation`, `test_detection_manual_confirm`) använder nu den delade `InMemoryDBStore`. Nytt cross-service-test (`test_cross_service_coherence.py`) bevisar poängen med migreringen: en rename via `ManagementService` syns omedelbart i `DetectionService`:s nästa matchning (och vice versa) — utan `reload_database`-anrop — eftersom båda tjänsterna delar samma store. Statistiktjänsten migreras separat (D4).
- **`ManagementService` migrerad till `FaceDBStore` (Fas D, steg D2).** Tjänsten höll tidigare egna kopior av de fyra DB-kollektionerna, laddade i `__init__`, med en `threading.Lock` och en 2,0 s TTL-cache (`reload_database`) samt synkrona `save()`-anrop. All den maskineriet togs bort: läsningar går nu genom `store.read(fn)` (iterationer/aggregeringar: `get_database_state`, `find_duplicate_people`, `find_redundant_encodings`, `list_distinct_pairs`, `get_recent_files`, existenskoll i `add_distinct_pair`) och muteringar genom `store.mutate(fn)` (`rename_person`, `merge_people`, `delete_person`, `move_to_ignore`, `move_from_ignore`, `undo_file`, `purge_encodings`, `dedup_people` när ej dry-run). Varje användarbekräftad, hållbarhetslovande mutering anropar `store.flush()` innan svar (samma synkrona persistens som gamla `save()`), så on-disk-tillståndet är skrivet när endpointen returnerar. Muteringar redigerar kollektionerna på plats (`d[k]=…`, `lst[:]=…`, `del d[k]`) eftersom store:t håller referensen. Distinct-pairs-registret (`distinct_pairs.json`) och dess hjälpare lämnas oförändrade — de rör en egen fil och skyddades inte av det borttagna låset. Freshness sköts nu av store:ts fingerprint-invalidering (mtime+storlek) i stället för TTL: en extern skrivning syns vid nästa läsning utan tidsfördröjning. Publikt API oförändrat (rutter rör inte). Detektions- och statistiktjänsterna migreras separat (D3/D4). TTL-karaktäriseringstesterna (`test_reload_database_skips_within_ttl` / `_rereads_after_ttl`) ersattes av ett store-baserat freshness-test (`test_external_write_visible_on_next_read`, medvetet uppdaterat); `test_scan_reloads_before_pruning` ersattes av `test_scan_reconciles_against_fresh_names`. Övriga karaktäriseringstester (rename/merge/delete/ignore, persistens + distinct-pairs) är oförändrade och gröna. Hjälptesternas fixtures (`test_distinct_pairs`, `test_duplicate_people`, `test_dedup_encodings`) använder nu en delad `InMemoryDBStore` (i `tests/conftest.py`) som läser tjänstens live-kollektioner i stället för att stubba det borttagna `_reload_from_disk`/`save`. Efter granskning: `dedup_people` (ej dry-run) planerar först under `read()` och muterar/flushar bara när något faktiskt tas bort — återställer gamla `if total`-no-op-garden så en tom dedup-bekräftelse inte skriver om DB-filerna; tre nya real-store-tester spikar in-place-mutationskontraktet (`move_from_ignore` persisterar en krympt ignored-lista; `undo_file` krymper processed/known/ignored på disk; no-op-dedup schemalägger ingen save).
- **ImageViewer-renderloop optimerad (prestanda, inget synligt beteende ändrat).** Canvasens ritväg gjordes om utan att zoom-lägen, pan-clamping eller ansiktsrute-rendering ändrades: (1) **Resize skild från ritning.** `render()` satte tidigare `canvas.width/height` vid *varje* anrop, vilket omallokerade och rensade backing-storen även när bara pan/zoom ändrats. Backing-storen ägs nu av en dedikerad effekt nycklad på `[dimensions, dpr]`; `render()` använder `ctx.setTransform(dpr,0,0,dpr,0,0)` + `clearRect` i stället för `canvas.width=…` + `ctx.scale`. DPR-ändringar (skärmbyte mellan olika täthet) spåras via en `matchMedia('(resolution: Xdppx)')`-lyssnare som re-armas vid varje ändring. (2) **Tema-färgcache.** De fem `--face-*`-CSS-variablerna lästes via `getComputedStyle(document.documentElement)` i den heta ritvägen (5 forced style resolutions per frame). De läses nu en gång och cachas; cachen invalideras vid temaändring genom både `theme-changed`-eventet (theme-manager) och en `MutationObserver` på `<html>` `data-theme`+`style` (fångar ThemeEditors live-preview som skriver inline-variabler utan event). (3) **Asynkron avkodning.** Bildladdaren gör nu `await img.decode()` och, där tillgängligt, `createImageBitmap(img, { imageOrientation: 'from-image' })` (EXIF-orientering appliceras — matchar HTMLImageElement-ritningen och backendens EXIF-bakade ansiktskoordinater; `close()` av föregående bitmap, HTMLImageElement-fallback), så första målningen inte avkodar synkront på main-tråden. Laddaren fick också en monoton generations-token: varje await (NEF-konvertering, decode, bitmap) re-checkar tokenen så en långsammare äldre laddning aldrig kan skriva över en nyare vid snabb navigering (förloraren stänger sin bitmap och avbryts tyst med `superseded`-markerad reject; `clear-image` invaliderar också in-flight-laddningar). (4) **JIT-uppvärmnings-hacket borttaget** (fejk-zoom 1.0001 100 ms efter laddning). (5) **Fit-to-canvas-matematiken utbruten** till ren `frontend/src/renderer/shared/fitTransform.js` (`computeFitTransform`) med enhetstester (landskap/porträtt/mindre-än-canvas/lika-ratio/DPR).
- **Delade filändelse-konstanter utbrutna till `frontend/src/renderer/shared/fileExts.js` (refaktorering).** `RAW_EXTS`/`JPG_EXTS` och en byte-identisk `extOf()` var verbatim-duplicerade mellan `trashGroup.js` och `CullingModule.jsx`. De ligger nu i en enda delad modul som båda importerar; `CullingModule` behåller sin egna `isRaw` (som medvetet använder `i !== -1` i stället för `extOf`:s `i > 0`). `FileQueueModule`:s `SUPPORTED_EXTENSIONS` lämnades orörd — den beskriver en annan domän (köbara filtyper: png/tiff ingår, flera RAW-format saknas) och delar inte RAW-vs-JPG-klassificeringens semantik. En kommentar i den nya modulen pekar på backendens kanoniska lista (`file_resolver.py`, `EXTENSION_PRESETS["raw"]`) som måste hållas i synk. Nya enhetstester (`frontend/tests/fileExts.test.js`) täcker `extOf`-kantfall och mängdmedlemskap. Noterat i granskningen av #119 (Nagelfar issue-001).
- **`faceid_db.py` flyttad in i `core/`-paketet som `core/db.py` (move-only).** Audit-steg 4: datalagret (pickle + jsonl) flyttades via `git mv` till `backend/core/db.py` utan logikändring. `backend/faceid_db.py` är nu ett bakåtkompatibilitetsskal som aliasar sig självt till `core.db` i `sys.modules` (`sys.modules[__name__] = core.db`), så `import faceid_db` och `import core.db` refererar till *samma* modulobjekt — attributåtkomst och testernas monkeypatchning (`test_faceid_db.py` patchar sökvägskonstanter) förblir koherenta över båda namnen medan de ~19 importörerna migrerar. `test_faceid_db.py` samt de core-interna importörerna (`core/config.py`, `core/naming.py`) pekar nu på `core.db`; övriga importörer fungerar oförändrat via aliaset. PyInstaller-specens `hiddenimports` utökad med `core.db`.
- **En enda fil-hash-implementation (avdupliceras).** De två egna SHA1-fil-hash-implementationerna i `api/services/detection_service.py` (`_get_file_hash`) och `api/services/preprocessing_cache.py` (`compute_file_hash`) ersätts av delegering till den kanoniska `core.db.get_file_hash`. De tre var verifierat algoritmiskt identiska (SHA1 över samma bytes; chunk-storleken 8192 vs 65536 påverkar inte digesten — bevisat mot en sampelfil). De tunna wrapper-metoderna behålls eftersom klass-API:t anropas externt (`routes/detection`, `routes/preprocessing`); anroparna kontrollerar filens existens innan, så den kanoniska implementationens `None`-vid-fel-väg nås aldrig i praktiken (beteendebevarande).
- **Kanonisk källa för RAW/JPG-filändelser (`backend/core/files.py`, avdupliceras).** De fyra kopiorna av RAW-ändelsemängden — `detection_service._load_image`, `routes/preprocessing._RAW_EXTS`, `file_resolver.EXTENSION_PRESETS["raw"]` och `rename_service.SUPPORTED_EXTENSIONS` (kommentarerna medgav "keep aligned with…") — ersätts av import från nya `core/files.py` (`RAW_EXTENSIONS`, `STANDARD_EXTENSIONS`, `JPEG_EXTENSIONS`, `SUPPORTED_EXTENSIONS`). Mängderna var identiska mellan sajterna (RAW=9, supported/all=14) och är oförändrade efter flytten (per-sajt diffade före/efter). De två site-specifika delmängderna i `rename_service._extract_datetime_original` (EXIF-hjälparens PIL-/rawpy-grenar) lämnades orörda för att inte tyst ändra någon sajts accepterade ändelser. Följdstädning: det redundanta `sys.path`-hacket i `culling_service.py` togs bort (backend-roten är redan på path för `cli_config` — verifierat med import utan hacket).
- **Statistik-/räknekärnan lyft ut ur `rakna_spelare.py` till `backend/core/playerstats.py` (move-only).** Audit-steg som flyttar den delade, rena logiken (statistik + exkluderings-config) ur CLI-skriptet så FastAPI-backenden och CLI:t har en enda sanningskälla. Flyttade oförändrat till `core/playerstats.py`: `parse_filename`, `build_entries`, `segment_matches`, `bucket_counter`, `classify_deviation`, `summarize_counter`, `compute_baseline`, `compute_player_stats`, `load_exclusion_config`, `save_exclusion_config`, `resolve_exclusion_sets`, `resolve_always_markers` samt konstanterna `ALWAYS_GRUPP`/`ALWAYS_PUBLIK`/`CONFIG_DIR`/`CONFIG_FILE`. Terminalrendering (`render_bar`/`render_spark`/`print_section`), `Colors` och argparse/`main` blir kvar i `rakna_spelare.py`, som nu importerar från `core.playerstats` och re-exporterar de flyttade namnen (via `__all__`) för bakåtkompatibilitet. De tre API-tjänsterna (`player_count_service`, `file_resolver`, `culling_service`) importerar nu direkt `from core.playerstats import …`; de `sys.path.insert`-hack som bara fanns för att nå `rakna_spelare` togs bort ur `player_count_service` och `file_resolver` (culling behåller sitt hack — det behövs fortfarande för backend-rotens `cli_config`-skal). Paketering: `core.playerstats` tillagt i PyInstaller-specens `hiddenimports`. Testernas monkeypatch-mål (`CONFIG_DIR`/`CONFIG_FILE`, `load_exclusion_config`) pekar nu på `core.playerstats` där funktionerna bor.
- **`log_attempt_stats` och namn-/filnamnshjälparna utlyfta ur `hitta_ansikten.py` till `core/` — API:t importerar inte längre CLI-monoliten (move-only).** Audit-steg 2: `log_attempt_stats` flyttades till nya `backend/core/attempts.py`, och den rena namnklustret (`extract_prefix_suffix`, `is_unrenamed`, `collect_persons_for_files`, `normalize_name`, `split_fornamn_efternamn`, `resolve_fornamn_dubletter`, `build_new_filename`) till nya `backend/core/naming.py`. Funktionskropparna är oförändrade; `hitta_ansikten.py` importerar tillbaka de namn den faktiskt använder. `api/services/detection_service.py` importerar nu `load_config` från `core.config` och `log_attempt_stats` från `core.attempts` i stället för från `hitta_ansikten` — efter detta finns inga imports av CLI-monoliten kvar under `backend/api/`. `api/services/rename_service.py` avdupliceras: dess byte-identiska `normalize_name` ersätts av `from core.naming import normalize_name` (åter-exporteras därifrån för `manual_suffix_service`). De övriga portade namn-hjälparna i `rename_service` har medvetet avvikit från monolitens (fotografsuffix i regex, config-parametrar) och lämnas orörda. PyInstaller-specens `hiddenimports` utökad med `core.attempts` och `core.naming`.
- **`core`-paket infört; `cli_config`/`cli_matching`/`cli_image` flyttade dit (move-only).** Audit-steg 1 av att lyfta ut delad logik ur CLI-modullagret: modulerna flyttades via `git mv` till ett nytt `backend/core/`-paket (`core/config.py`, `core/matching.py`, `core/image.py`) utan någon logik- eller funktionsnamnsändring. De gamla sökvägarna finns kvar som tunna bakåtkompatibilitetsskal (`backend/cli_config.py` m.fl. → `from core.<modul> import *`) så befintliga importörer (`hitta_ansikten.py`, `api/services/culling_service.py`, arkiverade skript) fortsätter fungera oförändrat. Paketering uppdaterad: `packages = ["core"]` i `backend/pyproject.toml` och `core.*` tillagt i PyInstaller-specens `hiddenimports`. Culling-retentiontestet pekar nu på `core.config` i stället för skalet (monkeypatch måste träffa den riktiga modulen där `load_config` läser `BASE_DIR`/`CONFIG_PATH`).
- **Orphan CLI-skript arkiverade till `backend/scripts/archive/`.** Åtta fristående legacy-/engångsverktyg utan runtime-importörer (verifierat via grep i audit 2026-07) flyttades ut ur `backend/`-roten: `analysera_ansikten.py`, `hantera_ansikten.py`, `forfina_ansikten.py`, `ratta_ansikten.py`, `rensa_dlib.py`, `migrera_processed.py`, `inspect_encodings.py` och `update_encodings_with_filehash.py`. Inget raderades — de förblir körbara. Varje flyttat skript som importerar backend-moduler (t.ex. `faceid_db`) fick en liten `sys.path`-bootstrap i toppen som lägger till `backend/` (tre nivåer upp), så de kan köras direkt: `python scripts/archive/<verktyg>.py` från `backend/`. En `backend/scripts/archive/README.md` beskriver vad de är, varför de arkiverades och hur de körs. Följdändring: dokumentationens sökvägar till `rensa_dlib.py` m.fl. pekar nu på `scripts/archive/…`. `filer2mappar.py`, `rakna_spelare.py`, `rename_nef.py` och `hitta_ansikten.py` är fortsatt aktiva och lämnades i `backend/`-roten.

### Removed
- **dlib startup dry-run scan borttagen.** Servern körde vid varje boot en full-DB-genomsökning (`_check_dlib_encodings`) som letade efter föråldrade dlib-encodings och, om några hittades, loggade en varning + skickade en persistent svensk frontend-notis om att köra `rensa_dlib.py`. Skanningen är nu borttagen: en hel-DB-scan per uppstart elimineras (snabbare boot), och den svenska notis-wiringen tas bort med den. Borttagningsverktygen finns kvar och körs på begäran — `rensa_dlib.py` och remove-dlib-endpointen i refinement.
- **`face_recognition`-beroendet borttaget ur `backend/pyproject.toml`.** InsightFace är enda aktiva backend; `face_recognition` (som bygger dlib från källkod) importeras numera bara lat inuti `DlibBackend.__init__`, så paketet är optionellt och behövs inte för normal drift. `DlibBackend` och `rensa_dlib.py` behålls som borttagningsverktyg. Följdändring: CI- (`ci.yml`) och release- (`release.yml`) workflows installerar inte längre systempaketen `cmake`/`libdlib-dev` — backend-jobbet slipper därmed dlib-bygget och blir snabbare.
- Removed the dead `backend/api/services/db_service.py` stub (a ~103-line all-TODO `DatabaseService` scaffold with a module-level singleton, added in the initial Phase 2 backend commit and never implemented — nothing imported it). Also dropped its `api.services.db_service` PyInstaller `hiddenimport` from `ansikten-backend.spec` and the stale `# TODO: Implement using db_service` reference in `routes/status.py`.
- **Dead pre-FlexLayout renderer files removed from `frontend/`.** The running app loads `src/renderer/workspace-flex.html` + `src/preload/preload.js` (wired in `src/main/index.js`), not the root-level renderer that predated the FlexLayout workspace. Deleted the now-unreferenced `frontend/index.html`, `frontend/renderer.js`, `frontend/preload.js`, `frontend/styles.css` and the orphaned `src/renderer/hooks/useCapabilities.js` hook (its `hooks/index.js` barrel export was removed too). Follow-ups: `package.json` `build.files` no longer lists the four dead root files (packaged builds relied on `src/**/*` + `main.js` anyway), and the ESLint renderer glob dropped its `renderer.js`/`preload.js` entries. `frontend/main.js` (the real Electron entry, `"main": "main.js"`) is kept.
- Removed root-level `RENAME_MANUAL_FACES_PLAN.md` (a completed bug post-mortem — its fixes are recorded in this changelog) and `.luarc.json` (added in 2b67010 to silence Lua LSP false positives from `.venv`/`dist`; deliberately dropped now as editor-specific config that belongs in a personal global ignore rather than the repo).

### Docs
- **`docs/dev/theming.md` synkad mot `theme.css`.** Rättade `theme.css`-headerpekaren (pekade på obefintlig `frontend/docs/styling-reference.md` → `docs/dev/theming.md`). I dokumentet: `--bg-active` (light) rättad till `#c8a060`, `--text-on-accent` till `#0a0a0a`, och de drivna `--color-*-bg`-värdena (light) uppdaterade till faktiska värden. Lade till ~15 tidigare odokumenterade tokens: `--accent-primary-alpha-20`, `--color-success/-warning/-error-text`, `--bg-row-hover`/`-active`, `--dropdown-selected-bg`, hela `--face-*`-uppsättningen (inkl. de nya konfidens-tokens), `--overlay-bg`/`-text`, `--progress-*`, `--btn-secondary-hover-bg`/`--btn-icon-hover-bg` och `--z-*`-skalan. `Component Patterns`-snuttarna som beskrev de icke-existerande klasserna `.btn`/`.btn-primary`/`.btn-ghost`/`.badge` ersattes med de faktiska klasserna `.btn-action`/`.btn-secondary`/`.btn-danger`/`.btn-icon`.
- **Dev-docs slutsvep efter kodgenomgången (audit #122–#162).** `docs/dev/architecture.md` skrevs om mot verkligheten: `core/`-paketet med `cli_*`/`faceid_db`-skal, `FaceDBStore` som enda DB-auktoritet (read/snapshot/mutate(touches)/flush, leading-coalesce-debounce, versionsräknare, mtime+storlek-invalidering), `MatchingIndex` (versionsinvaliderat), lata service-getters och schema-markör-migreringen (`db_meta.json`), samt den dekomponerade frontend-strukturen (workspace-moduler, `review/`/`culling/`/`fileQueue/`, delade hooks inkl. `useActiveTabset` med companion-gating). `docs/dev/database.md`: `db_meta.json`, `manual_suffixes.json`, `distinct_pairs.json`, flock/atomisk-rename, per-kollektion-sparningar (`only=`), `RestrictedUnpickler` och schema-markör-migreringen dokumenterade; det oanvända `metadata.json` noterat som legacy. `docs/dev/api-reference.md`: de två tidigare odokumenterade endpointsen `POST /api/v1/batch-confirm` och `POST /api/v1/statistics/file-stats` tillagda. `docs/dev/performance-plan.md` stängdes ut — levererade poster märkta med PR-referenser, kvarvarande arbete (concurrency-limits, auto-paus av bakgrundsrefresh) samlat och spårat i ROADMAP. `docs/dev/onboarding.md` + `contributing.md` svepta för inaktuella sökvägar/modulnamn/testkommandon. `frontend/README.md` fick en minimal användbar kropp; fil-strukturträden i `CLAUDE.md` + `frontend/CLAUDE.md` uppdaterade till post-dekompositions-verkligheten.
- README: corrected the Python requirement (3.11+, matching `pyproject.toml`), the dev install command (`pip install -e ".[dev]"`), and the feature list (added Importera, Räkna spelare, Gallra spelare and Papperskorg).
- **API-path och backend-tabell rättade i dokumentationen.** Alla routrar monteras under `/api/v1`, men CLAUDE.md (arkitektur + Module Communication-exemplet) och delar av `docs/dev/api-reference.md` (Database/Statistics/Management/Preprocessing/Files/Refinement samt Face Detection-avsnittet) angav sökvägar utan `/v1`. Sökvägarna är nu normaliserade till fullständiga `/api/v1/...` genomgående (utom `/health` och `ws://.../ws/progress` som ligger i roten). CLAUDE.md:s route-träd listar nu även de faktiska route-filerna (`player_count`, `culling`, `imports`, `rename_nef`), och backend-tabellen anger InsightFace som enda backend (kvarvarande dlib-encodings detekteras vid serverstart med en varning om att köra `rensa_dlib.py`; inget raderas automatiskt), i stället för att lista dlib som ett levande legacy-backend.

### Fixed
- **Mörkt tema: varningsrutan i `RefineFacesModule` använde obefintliga tokens.** `.warning-message` refererade `--bg-warning`/`--status-warning`/`--text-warning` med hårdkodade ljusa fallbacks (`#fff3cd`/`#ffc107`/`#856404`) — variablerna finns inte i `theme.css`, så fallbacken slog alltid till och rutan blev ljus även i mörkt tema. Bytt till de faktiska temanycklarna `--color-warning-bg`/`--color-warning`/`--color-warning-text` (utan hårdkodade fallbacks), så rutan följer båda teman. Samma fantomtoken-referens i `.migration-section` städades bort (använder nu `--bg-elevated`/`--border-medium` direkt, oförändrat utseende), liksom i `FileQueueModule.css` där `.preprocessing-status .status-paused` refererade `--text-warning` utan fallback (deklarationen föll bort — pausad-etiketten ärvde omgivande färg); nu `--color-warning-text`. Observera att även ljusa temat rättas på två ställen: varningsrutan (som var Bootstrap-gul, inte tema-beige) och `OriginalView`:s letterbox (som var en hårdkodat mörk rektangel i beige container).
- **Garanterad aktiv tabset efter varje layoutladdning (täpper till kvarvarande dubbel-trash-lucka).** `isTabsetActive`-gaten failar avsiktligt ÖPPET när FlexLayouts `getActiveTabset()` är `undefined` (annars skulle en aldrig-klickad enpanelslayout tappa sina kortkommandon). Men en återställd handbyggd split där både Review och Culling syns *utan* aktiv tabset lät båda tangentgatarna öppna samtidigt — ett enda `Cmd+⌫` kunde då nå båda, precis buggen #159 fixade. Ny ren hjälpare `ensureActiveTabset(model)` (`frontend/src/renderer/workspace/flexlayout/tabsetUtils.js`) sätter en deterministisk aktiv tabset direkt efter varje `Model.fromJson` när ingen är aktiv: den maximerade tabsetten om layouten återställde en, annars samma mål som `resolveTargetTabset` väljer (största renderade yta, eller högsta vikt före första mät-passet) — huvudarbetsytan. Anropas vid alla fyra modell-skapanden (initial laddning, fallback, förinställnings-växling, solo-modul). `resolveTargetTabset`s null-gren behålls (hängslen och livrem) men är nu i praktiken död i normalflödet. Fäst av nya enhetstester (`resolveTargetTabset.test.js`) och en dokumenterande fail-open-test i `activeTabset.test.js`.
- **Modultangenter villkoras på aktiv tabset, inte bara synlighet (fixar möjlig dubbel-trash).** Globala keydown-lyssnare gatades tidigare enbart på `node.isVisible()`, så i en delad layout kunde en *synlig men inaktiv* panel fånga tangenter som hörde till den aktiva. Konkret band både `ReviewModule` och `CullingModule` `Cmd+⌫` på var sin visibility-gate:ad lyssnare utan korsavbrytning — med båda synliga kunde ett enda `Cmd+⌫` mjukradera två filer (#118). Ny delad hook `frontend/src/renderer/hooks/useActiveTabset.js` besvarar "äger min flik tangentbordet nu?" genom att läsa FlexLayout-modellens aktiva tabset live vid varje tangenttryck: synlig **och** (egen tabset aktiv **eller** en *companion* hostar den aktiva tabsetten). Review anger `image-viewer`/`original-view` som companions så det normala flödet — klicka i bilden (bildvisarens tabset blir aktiv) och tryck sedan bekräfta/navigera — fortsatt går till Review; companions är tangentbordslösa så det kan inte återinföra korsprat. Culling är självständig (inga companions) och äger tangenter bara när den är aktiv tabset. Resultat: med Review och Culling synliga i olika tabsets når ett `Cmd+⌫` exakt en modul (den aktiva). `FileQueueModule`s `n`/`p` lämnas visibility-gate:ade (ingen aktiv konflikt; kräver companion-modellering — spårat i ROADMAP). Fäst av nya tester (`activeTabset.test.js` — predikat + ömsesidig uteslutning; utökade `reviewModule.test.jsx`/`cullingModuleFence.test.jsx`).
- **Backend-tjänstens livscykel i Electron main: env-gated debug, snabbare ready-poll, tillförlitlig avstängning (`frontend/src/main/backend-service.js`).** Tre fixar i backend-auto-starten: (1) `const DEBUG = true` var hårdkodad → all verbose backend-loggning var alltid på i produktion. Den läses nu från miljön (`process.env.ANSIKTEN_DEBUG === '1'`), av som standard; sätt `ANSIKTEN_DEBUG=1` för att få tillbaka loggarna (samma filtrering av `GET /api/v1/statistics/summary 200`-brus när på). (2) `waitForReady()` sov en fast `setTimeout(2000)` innan första health-pollen — garanterade +2 s på varje uppstart. Den förhandssömnen är borttagen: första pollen sker direkt, sedan samma retry-loop (oförändrad total timeout; tidiga `ECONNREFUSED` sväljs fortfarande tyst medan backenden bootar). (3) `stop()`:s 5 s SIGKILL-fallback läste `this.process` inne i timer-closuren, men `start()`:s exit-handler nollar `this.process` när processen dör → fallbacken kunde bli en no-op och lämna python-backenden vid liv. `stop()` fångar nu `const proc = this.process` vid entry och använder handtaget i closuren, rensar force-kill-timern när processen avslutas rent, och `unref()`:ar timern så en väntande timer inte håller Electron-main vid liv vid quit.
- **`api-client`: abort-listener-läcka och `clearCache` utanför klientens felhantering.** `_fetchWithTimeout` la till en `abort`-lyssnare på en externt inskickad `AbortSignal` men tog aldrig bort den — upprepade anrop med en långlivad signal (t.ex. en delad kö-controller) ackumulerade lyssnare. Lyssnaren registreras nu med `{ once: true }` och tas bort i ett `finally` efter att anropet settlat, och en signal som redan är avbruten före anropet kortsluter direkt (avbryter utan att lägga till någon lyssnare). Dessutom gick `clearCache()` förbi klientens plumbing: den använde rå `fetch` + generiskt `throw new Error(...)` i stället för `_fetchWithTimeout` + `_classifyError`. Den routas nu genom samma väg som `get`/`post`, så timeout och felklassificering (t.ex. `NetworkError` med `isTimeout`/`statusCode`/`retryable`) beter sig konsekvent. Enda anroparen (`PreferencesModule.handleClearCache`) loggar bara felet och påverkas inte av den ändrade felformen. Nya enhetstester i `frontend/tests/apiClient.test.js` täcker timeout-abort, extern signal-abort, lyssnar-städning, redan-avbruten signal och `clearCache`-konsekvens.
- **Committade versionsnummer synkade med utgivna 1.3.0.** `backend/pyproject.toml` (och `version=`-strängen i `backend/api/server.py`, som `/health` returnerar) låg kvar på `1.0.0` medan `frontend/package.json` och den senast utgivna releasen är `1.3.0` — så en fristående backend rapporterade fel version. Båda backend-förekomsterna är nu bumpade till `1.3.0`. Release-bygget skriver ändå över frontend-versionen från `v*`-taggen (`npm pkg set version`), vilket nu är dokumenterat i [release-guide.md](docs/dev/release-guide.md) och [building.md](docs/dev/building.md) tillsammans med regeln att bumpa alla committade versioner ihop. Den kvarvarande trippel-dubbleringen av versionssträngen är noterad i ROADMAP.
- **`init_logging` kraschade när datamappen saknades (fresh machine / CI).** `init_logging()` körs vid import av `hitta_ansikten` — före `load_config()` som skapar `BASE_DIR` (`~/.local/share/faceid/`) — och `logging.FileHandler` kastade `FileNotFoundError` när mappen inte fanns. Loggningen skapar nu logfilens föräldramapp (`mkdir(parents=True, exist_ok=True)`) innan filhandtaget öppnas.
- **`cli_config` kraschade vid import på icke-macOS (t.ex. Linux CI).** `TEMP_DIR` var hårdkodad till `/private/tmp/ansikten` och gjorde `mkdir(parents=True)` vid import — sökvägen finns inte och går inte att skapa på Linux, så varje modul som importerar `hitta_ansikten`/`detection_service` kraschade med `PermissionError: '/private'`. `TEMP_DIR` väljs nu efter plattform: `/private/tmp` på macOS (den paketerade appen tillåter `/tmp` och `/private/tmp`, men inte systemets temp `/var/folders/…`), annars `tempfile.gettempdir()`. macOS-beteendet är oförändrat.
- **JPEG-rotation: felplacerad ansiktsruta och roterad miniatyr.** Foton med EXIF-orientering (t.ex. mobil-JPEG tagna i porträtt, `Orientation = Rotate 90 CW`) fick ansikten detekterade i det oroterade pixelplanet medan frontend (Chromium `<img>`) visar bilden EXIF-roterad — så bounding box-rutan hamnade ~90° fel (bredvid ansiktet) och miniatyren i sidopanelen visades liggande. Backend `_load_image` applicerar nu `PIL.ImageOps.exif_transpose()` innan pixlarna läses, så detektion och miniatyrbeskärning sker i samma orienterade plan som visas. RAW/NEF påverkades inte (libraw orienterar redan). Not: ansiktsminiatyrer som redan cachats på disk från en tidigare preprocessing av roterade JPEG:ar kan behöva regenereras.
- **Kvarhängande engelska flik-namn ("Image Viewer", "File Queue").** Fliktitlarna hade svenska översättningar ("Bildvisare", "Filkö") men visades ändå på engelska för den som sparat en layout innan översättningarna landade: FlexLayout serialiserar varje fliks `name` i den sparade layouten, och `Model.fromJson` kör aldrig om `t()` vid återställning. Layouten som läses från `localStorage` går nu genom `retranslateTabNames`, som återapplicerar aktuell översättning på varje känd modulflik (nyckel: flikens `component`/`moduleId`). Okända/manuellt omdöpta flikar lämnas orörda.

### Added
- **`FaceDBStore` — process-wide face-DB repository (`backend/api/services/db_store.py`), not yet wired.** First step of Phase D: unifying the three independent in-memory copies of the face DB (detection_service, management_service, statistics_service) behind ONE store. Introduces the store + tests only — no service is migrated onto it yet. The store holds the four collections from `core.db.load_database()` (`known_faces`, `ignored_faces`, `hard_negatives`, `processed_files`) plus a monotonic `version` (starts 0, bumps on every mutation and reload). `snapshot()` cheap-stats the four backing files' fingerprint (`st_mtime_ns`, `st_size`) and reloads only on external change (returning the LIVE objects — no deep copy, matching today's semantics; callers must mutate only via `mutate()`, and iterate/aggregate via `read()`); the residual same-mtime **and** same-size false-negative is documented as accepted. `mutate(fn)` runs `fn(known, ignored, hardneg, processed)` under a reentrant lock, passes the return value through, bumps the version and schedules a **leading-coalesce** save (`threading.Timer`): the write fires 500 ms after the FIRST mutation of a burst and is not re-armed by later ones — matching detection_service's `_schedule_save` semantics, so sustained mutation still saves roughly every 500 ms — and records its own post-save fingerprints so it never triggers a false external reload. `read(fn)` runs a read-only `fn` over the live collections under the lock (safe iteration/aggregation once services share the store in D2-D4). `flush()` cancels the pending timer and writes now if dirty (for durability/shutdown). Save goes through `core.db.save_database` (atomic rename + flock preserved); dirty-flag granularity is deferred to E1 (a save writes all four files). Singleton via a lazy `get_db_store()` (no import-time construction). 21 unit tests in `backend/tests/test_db_store.py` cover initial load, snapshot stability (spy asserts a single reload), external-write invalidation per file (including same-mtime/different-size), versioning, leading-coalesce timing + periodic saves under sustained mutation, `read()` (result passthrough, freshness check, mutual exclusion vs `mutate`), flush, no-false-self-reload, 1000-mutation read-modify-write concurrency (verified to fail without the lock), and RLock reentrancy — all against a temp dir with `core.db` path constants monkeypatched away from `~/.local/share/faceid`.
- **Committed lint configs + CI enforcement (Ruff + ESLint):** the repo had no committed linters. Backend now ships a `[tool.ruff]` config in `backend/pyproject.toml` (target py3.11, line-length 100, default `E`/`F` rules + import sorting `I`; `E402` ignored in `api/server.py` where routers are imported after app construction). Frontend ships an ESLint 9 flat config (`frontend/eslint.config.mjs`: `@eslint/js` recommended + `eslint-plugin-react-hooks` `rules-of-hooks` as error, `exhaustive-deps` off, browser/node/vitest globals per area; build output and `node_modules` ignored) with a `npm run lint` script. The **CI** workflow now runs `ruff check .` before `pytest` in the backend job and `npm run lint` before Vitest in the frontend job. `ruff` was added to the backend `dev` extra; `eslint` + plugins to the frontend dev dependencies. Config-first and pragmatic — the rules pass the existing code after safe autofixes (import sorting, dead-import/variable removal) rather than a wholesale reformat.
- **Karaktäriseringstester för `faceid_db` (`backend/tests/test_faceid_db.py`):** 27 tests pinning the current behavior of the data layer (pickle + jsonl) as a safety net for upcoming refactors — save/load round-trip against a temp dir (all path constants monkeypatched away from `~/.local/share/faceid`), `normalize_encoding_entry` (legacy bare arrays, dicts missing `backend`, passthrough, invalid-type → `None`), `get_file_hash` (known SHA1 + chunked reads for files > 64 KB), `rotate_logs` (no-op path, processed trim, attempt archive, oversized-log rotation via small synthetic limits), atomic write failure leaving the original intact with no `.tmp` litter, shared/exclusive flock usage, and `RestrictedUnpickler` blocking non-whitelisted classes.
- **Karaktäriseringstester för de tre tjänsterna (`backend/tests/test_detection_service.py`, `test_management_service.py`, `test_statistics_service.py`):** 41 tests pinning the current behavior of `DetectionService`, `ManagementService` and `StatisticsService` ahead of a refactor that moves them onto a unified `FaceDBStore`. Focused on the seams that refactor will touch rather than exhaustive coverage: **detection** — match-status decision logic (`_determine_match_case`), single-nearest matching with backend filtering (`_match_encoding`), detection-cache keying (file hash + distinct-pairs registry version), what `reload_database` clears, the confirm-identity mutation path (`known_faces` + `hard_negatives`), and debounced-save coalescing; **management** — rename / merge / delete / move-to-ignore against a real temp DB (asserting the persisted on-disk state and distinct-pairs registry follow-through), plus the 2.0 s TTL reload behavior; **statistics** — the `get_summary()` output shape for a synthetic DB + attempt log, and that it loads via `load_database`/`load_attempt_log` once per uncached request (spied via counters; marked with a NOTE since the refactor changes this). The InsightFace model is never loaded (fake backend), and all data-layer paths are monkeypatched away from `~/.local/share/faceid`.
- **CI-arbetsflöde för push och pull request (`.github/workflows/ci.yml`):** the repo previously only had the tag-triggered release workflow, so nothing gated `dev`/`master`. A new **CI** workflow now runs on every pull request and on pushes to `dev`/`master`, with two parallel jobs — **backend** (Ubuntu, Python 3.11: `pip install -e ".[dev]"`, then `pytest`) and **frontend** (Ubuntu, Node 20: `npm ci`, `npm test`, `npm run build:workspace`). A concurrency group cancels superseded runs on the same ref, and pip/npm caches speed up installs.
- **Granska ansikten — döp bilder utan ansikten manuellt:** when detection finds no faces, the review panel now shows a **"Lägg till namn manuellt"** button (and keeps keyboard focus) instead of a dead-end "Inga ansikten identifierade" message. Clicking it adds a manual name field — so a photo of an object (or a face the detector missed) can still be named and drive the filename, without the previously hidden `m` shortcut. Works the same in fix mode, which re-detects a processed file into this state. Uses the existing `addManualFace` flow (manual names already carry through to the rename pipeline).
- **Fritt filnamnstillägg (`Alt+Enter`) i Granska ansikten:** you can now attach a free-text suffix to the file you're reviewing (e.g. "vinbär" → `260401_140101_Vinbar.jpg`). It is **not** a person name — it never touches the face DB (`encodings.pkl`), name autocomplete, or the person pipeline; it only affects the filename. `Alt+Enter` opens a small dialog (prefilled with any existing suffix) with a live, normalized preview; `Enter` saves, `Esc` cancels. Normalization mirrors the person-name rules: spaces → `_`, diacritics folded (å/ä→a, ö→o), path-unsafe chars sanitized, repeated `_` collapsed. When the image also has confirmed faces the suffix is appended **after** the names; a faceless photo with only a suffix still renames. The suffix is persisted per file in a dedicated store (`manual_suffixes.json` under the faceid data dir) keyed by content hash (SHA1, stable across rename), separate from `attempt_stats`/`encodings`. New endpoints `GET`/`POST /api/v1/files/manual-suffix`; the rename preview/execute path reads the store and threads the suffix into filename building. Client normalizer `shared/manualSuffix.js` (unit-tested) keeps the preview in sync with the backend.
- **Radera aktuell fil till papperskorgen (`Cmd+⌫`) från Granska ansikten:** you can now soft-delete the file you're reviewing without leaving the review flow. `Cmd+Backspace` (Finder-konvention; även Arkiv → "Flytta till papperskorgen") moves the reviewed file to the same app-managed, restore-backed trash that Gallra spelare uses (`/api/v1/culling/trash`), prunes it from the queue and auto-advances to the next eligible file. **`Cmd+Shift+⌫`** (Redigera → "Ångra radering") restores the most recent one to its place in the queue — a dedicated shortcut, not `Cmd+Z`, which stays Review's face-undo. The keys are handled by the review surface's own keydown (gated to when Granska ansikten is visible and not while typing in a name field) rather than a global menu accelerator, so they never shadow Gallra spelare's own `Cmd+⌫` or trash the wrong file. The trash request carries the exact reviewed path and is executed in one place (FileQueueModule), so it acts on the right file and fires once. Deleting discards any unsaved review of that file (it's being trashed).
- **Papperskorg som egen vy (nåbar från menyn):** the app trash (soft-deleted files with restore) was only reachable from inside Gallra spelare. It's now a standalone **Papperskorg** module openable from **Visa → Papperskorg** — list, filter by type (jpg/raw), restore individual files, or empty (all or the filtered subset). The trash view is extracted into one shared `TrashPanel` component used by both the new module and Gallra spelare (single source of truth; no duplicated logic), backed by the same `/api/v1/culling/trash|restore|empty` endpoints. Gallra spelare passes a callback so its cull-loop undo stack and file list stay in sync after a restore/empty. The `trashGroup` filetype classifier moved to its own module (`trashGroup.js`).

- **CLI import subcommand (`ansikten import [MÅL]`)**: completing the workflow-verb trio alongside `faces` and `culling`, the terminal launcher can now open the Import module directly. `ansikten import` opens/focuses Importera with the memory card autodetected as before; `ansikten import ~/foto/2026` additionally pre-fills that folder as the transfer destination (persisted like any manual edit). The destination is a *target* (not a source or file list) and need not exist yet — it's created on transfer — so `--clear`/`--recursive` are no-ops for import. When the app is already running the args route into the live window; a bare `ansikten import` still suppresses the startup landing (unlike a bare `culling`, since import always opens its module). Parsing stays in `cli-args.js` (unit-tested); the destination is handed over via a dedicated `open-import` IPC channel mirroring `open-culling`.
- **Gallra spelare — översiktsläge (rutnät, Lightroom-likt):** culling now has a thumbnail/contact-sheet overview mode alongside the single-image loupe. A **Rutnät** toggle button in the filter bar switches between them — highlighted while the overview is active (the choice persists across restarts). The grid runs over the same file list and selection as the loupe, so navigation, culling (`x`/Delete/`Cmd+⌫`), undo (`Cmd+Z`) and `L` (Lightroom) all work on the focused thumbnail. Movement is 2-D: `←`/`→` step one cell, `↑`/`↓` (`k`/`j`) move a row, `Alt`+arrow pages. **Double-click** or **Enter** on a thumbnail opens it in the loupe; **Esc** in the loupe (when nothing is being edited) returns to the grid. Clicking a player in the stats column is mode-aware: in the grid a **single click highlights** that player's thumbnails (the rest dim, no filtering) and a **double click filters** to them; in the loupe a single click filters as before. Per-player highlighting is derived client-side from filenames — no extra backend data. Thumbnails come from the `preview-thumb` endpoint below; the grid component is `CullingGrid.jsx`, the 2-D geometry is the unit-tested `culling-grid-nav.js`.
- **Overview-thumbnail endpoint (`GET /api/v1/preprocessing/preview-thumb`):** a whole-frame downscaled JPEG for a JPEG or RAW file (query `path`, `size`; default 256px, clamped 32–1024), returned as `image/jpeg` with `Cache-Control: public, max-age=604800` and `X-Cache: HIT|MISS`. RAW files use the embedded preview (`rawpy.extract_thumb`, milliseconds) with a full-decode fallback; EXIF orientation is honoured. Cached under a new `grid/` kind in `PreprocessingCache`, keyed on a cheap path+mtime+size fingerprint (no full-file hashing), so filling a grid of hundreds of files stays fast and an in-place re-export refreshes automatically. Frontend client `shared/grid-thumbnail-cache.js` (blob-URL LRU + `useGridThumbnail` hook) mirrors the existing face-thumbnail cache. Infrastructure for the upcoming Gallra spelare grid/overview view.
- **Räkna spelare — configurable "always excluded" markers:** the built-in always-excluded names (team/group photos `Laget`/`FBK`, crowd `Klacken`) are no longer hard-coded — they're config-driven (config keys `always_grupp`/`always_publik`, or env `RAKNA_ALWAYS_GRUPP`/`PUBLIK`), defaulting to the built-ins so existing setups are unchanged. In the **Uteslutna** editor these are now editable under an "Alltid uteslutna (sparas)" section (Gruppbilder + Publik (alltid)): add your own (e.g. a `Forward` group label) or drop a built-in, then **Spara som standard**. Configured group markers are shown (previously the group row was fixed/locked). When a `RAKNA_*` env var shadows the config, the editor warns that saving won't take effect. Backend: `save_exclusion_config` / `GET`·`POST /api/v1/players/exclusions` now handle `grupp`/`always_grupp`/`always_publik` and report `env_active`.
- **Startup landing returns on an empty workspace:** closing the last open module now brings the landing page back (instead of leaving a blank workspace), so there's always somewhere to go.
- **Startup landing — all views reachable:** the landing page now lists every view, not just the five workflow steps. The workflow steps (Importera · Byt namn · Granska ansikten · Räkna spelare · Gallra spelare) stay first, in order; below a **Verktyg** divider the rest are one click away — Databashantering, Förfina ansikten, Filkö, Statistik, Loggar, Inställningar, Temaredigerare — so you can jump straight to e.g. database management. Each opens filling the workspace (Granska ansikten still opens its review layout). Button labels come from the i18n catalog.
- **Swedish display language — remaining modules & stragglers:** completing the sweep, these are now Swedish too, each migrated to its own catalog namespace: **File Queue** (`fileQueue.*`, ~97 sites — toggles, buttons, status badges, preprocessing states, tooltips, toasts, rename dialog), **Refine Faces** (`refineFaces.*`, ~54 — filter modes, settings, table, reason-code display, messages), **Statistics / Log Viewer / Connection** (`statistics.*`/`logs.*`/`connection.*`), **Review / Image Viewer / Original View / Startup Status / Player Count** (`review.*`/`imageViewer.*`/`originalView.*`/`startupStatus.*`/`playerCount.*` — dialogs, status messages, toasts, badges), the **keyboard-shortcuts help** overlay (`shortcuts.*`), and the last **stragglers** (api-client error toasts `errors.*`, workspace shell `workspace.*`, and main-process splash/native dialogs `dialogs.*`). Kept as intentional technical/proper terms: `Mahalanobis`, `InsightFace`, preset names (Terminal Beige / CRT Phosphor), `CLI`, the `ign` abbreviation, `HTTP {code}` diagnostics, `Per match`. "backend" is read in context — the API server → **Servern**, the recognition engine → **Motorer**. The catalog is split into per-namespace files under `frontend/src/i18n/sv/`.
- **Swedish display language — Database Management:** the whole Database Management module is Swedish (title, section headers, operation titles, buttons, placeholders, empty states, validation/error/success toasts, and confirm dialogs), migrated to `database.*` with interpolation and plural forms. (~99 keys.)
- **Swedish display language — Theme Editor:** category names, colour/variable labels, theme-mode options, preset bindings, and Export/Import are now Swedish (`themeEditor.*`); Save/Reset reuse `common.*`. Preset brand names (Terminal Beige / CRT Phosphor) kept.
- **Swedish display language — Preferences:** the entire Preferences module is now Swedish (section labels, field labels + hints, dropdown options, the reset/clear confirmation dialogs), migrated to the i18n catalog under a `preferences.*` namespace; Save/Reset reuse the shared `common.*` strings. (~180 strings.)
- **Swedish display language — application menu:** the entire app menu (File/Edit/View/Window/Help, the Theme submenu, and the macOS app menu) is now Swedish, migrated to the i18n catalog (`menu.*`); module-open items reuse the shared `modules.*` names so menu and tab labels stay consistent. **"Öppna i Lightroom" moved from the View menu to the File menu** (Arkiv), where a file/export action belongs. (Menu labels live in the main process; the CommonJS catalog is shared with it.)
- **Swedish display language — i18n foundation:** introduced a lightweight, framework-free string catalog (`frontend/src/i18n/`, CommonJS so the Electron main process and the bundled renderer share one catalog) with a `t(key, vars)` helper (dotted keys, `{var}` interpolation, simple `{one, other}` plural, missing-key returns the key so gaps are visible). First migration: the workspace tab labels are now consistently Swedish — `MODULE_TITLES` and the `layouts.js` template tab names both derive from the catalog (e.g. "Image Viewer" → **Bildvisare**, "Face Review" → **Granska ansikten**, "Database Management" → **Databashantering**), fixing the half-English/half-Swedish tab strip. This is step one of a per-module sweep tracked in [ROADMAP.md](ROADMAP.md) ("Svenskt visningsspråk").
- **Räkna spelare — counting controls + exclusion editor (full CLI parity):** the module now exposes the remaining `rakna_spelare.py` parameters in the GUI. An options row under the input bar adds **Matchgap** (`--gap-minutes`, match splitting, default 30), **Baslinje** (`--baseline`, median/mean) and **Min bilder** (`--min-images`, default 3), all applied live. An **Uteslutna** panel edits the coach/audience (`tranare`/`publik`) exclusion lists — pre-filled from the resolved config/env defaults, editable per-request, with a **Spara som standard** button that persists them to `rakna_spelare.json` (so they also apply to future counts and the CLI). The always-markers `Laget`/`FBK`/`Klacken` are shown locked and can't be removed. New endpoints `GET`/`POST /api/v1/players/exclusions`; the `/players/count` request already accepted the counting params.
- **Gallra spelare — öppna original-NEF i Lightroom:** press `L` (or the right-click menu / View → "Öppna i Lightroom", `Cmd+Shift+L`) on a photo in culling to open its source NEF in Adobe Lightroom. The developed JPEG and its NEF live in different folders and don't share a full filename, so the NEF is resolved by the shared leading `YYMMDD_HHMMSS[-N]` timestamp token, searched recursively under a configurable RAW root (Preferences → Files → "RAW-mapp (NEF)", default `~/Pictures/nerladdat`). Exact-token match (a plain timestamp won't open a burst `-N` sibling); the first match is opened (Lightroom shows it in-folder). macOS only. New IPC `open-raw-in-lightroom`; matching logic in `src/main/raw-match.js` (unit-tested).
- **Räkna spelare — per-match view now complete:** with "Per match" on, each match now shows the same detail as the session view (and the `rakna_spelare.py -p` terminal output) does — an info row with the player count, excluded count, and the match's baseline (`Baslinje (median): X`), plus the collapsible excluded buckets (Tränare / Gruppbilder / Publik / Under tröskeln). A match that contains only excluded people (e.g. a team or crowd shot) now reports them instead of appearing empty. Pure frontend rendering of data `/players/count` already returns.
- **Räkna spelare — full CLI parity in the table:** the player table now matches the `rakna_spelare.py` terminal output — a per-player **temporal sparkline** (a density histogram of when that player was photographed across the session, and per match when "Per match" is on), an absolute **ΔN** column (image-count deviation from the baseline) alongside Δ%, and a **baseline-relative distribution bar** (at-baseline fills half the track, 2× fills it, with a baseline tick) instead of the previous max-relative bar. All from data the `/players/count` endpoint already returns — no backend change.
- **Räkna spelare ↔ Gallra spelare share the same file selection:** opening one now adopts the other's current scan scope (folders, path-globs, date span, recursion, extension preset) instead of starting empty — so after culling a folder you can open Räkna spelare and immediately see its counts for the same files, and vice versa. Only the scan scope is mirrored, not culling's per-player filter (Räkna spelare counts everyone). Shared in-memory for the session.
- **Räkna spelare — always-excluded markers (`FBK` / `Klacken`):** the photographer's labels for team photos (`FBK`, alongside the built-in `Laget`) and crowd shots (`Klacken`) are now always treated as group/audience exclusions, never counted as players, regardless of image count or config — in both the CLI and the GUI/API. The group (`grupp`) exclusion list is now configurable too (config-file `grupp` key + `RAKNA_GRUPP` env), with `Laget`/`FBK` always merged in and `Klacken` always in `publik`. Coach/audience/group resolution is now a single shared function so the CLI and GUI never diverge.
- **Gallra spelare — auto-advance after rename:** renaming a file in culling (inline `Enter` rename or the `Cmd+Enter` name removal) now advances to the next file, so you can keep moving through the set. Configurable under Preferences → Files → Gallra spelare ("Auto-advance after rename"), default on.
- **Gallra spelare — snabb bortbockning av namn:** the preview pane shows the current file's player names as checked chips in an overlay at the top. Unchecking a name **previews the resulting filename live in the left file list** (the current row turns orange while the change is uncommitted — shown once, in the list, not duplicated in the overlay); `Cmd+Enter` commits the rename for real. Faster than manual inline rename when a developed JPG was cropped so one player is no longer in frame. Removal is filename-only (splits the `,_`-joined names, drops the toggled-off pieces, rejoins) — it never adds names; removing all leaves a bare timestamp. `Enter` still opens the full manual inline rename. **Navigating away with an uncommitted toggle prompts a dialog** — `Cmd+Enter` saves, `Enter` discards (default), `Esc` cancels — so a pending rename isn't lost silently.
- **Gallra spelare — `Cmd+⌫` to cull:** following Finder's "move to trash" convention, `Cmd+Backspace` now culls the current file (alongside the existing `x` and `Delete`).
- **CLI subcommands (`ansikten culling` / `ansikten faces`)**: the terminal launcher now selects a workflow via a verb instead of always feeding files into the face queue. `ansikten culling DIR` (alias `cull`) opens/focuses the Gallra spelare module and loads the folder; `ansikten faces *.NEF` (or bare `ansikten *.NEF`) queues for face review as before. `--clear`/`-c` resets the target's working set before adding (alone = just empty); without it, paths are appended to the existing set. `--recursive`/`-r` (culling) scans sub-folders too — the default is just the named folder, matching shell-glob intuition. When the app is already running the args are routed into the live window (the right module opens/focuses). The launcher is now a versioned script in the repo (`bin/ansikten`) replacing the previous zsh function; all argument parsing lives in `src/main/cli-args.js` (unit-tested).
- **Startup landing page**: when the app opens with an empty workspace, a centered view presents the workflow steps in order (Importera · Byt namn · Granska ansikten · Räkna spelare · Gallra spelare) as buttons that open the matching module. Import is enabled only while a camera card is mounted (polled), the rest are always available. The view disappears once a module opens or an image loads.
- **Räkna spelare** module: counts images per named player from filenames (no face recognition) with median-baseline over/under-representation stats; folder/glob input with extension presets and a filename-date span; live auto-refresh when the watched folder changes (#46).
- **Gallra spelare** culling workspace: filter by player or a Finder-style glob, file list beside a maximized preview, keystroke culling (`x`/Delete) with auto-advance and `Cmd+Z` undo, backed by an app-managed trash with restore-to-original (#46). Extended to NEF/RAW via the existing NEF→JPG preview pipeline, with debounced conversion on fast stepping (#47).
- Folder-level file-watching IPC and a folder-path dialog, shared by the new modules.
- **Importera** module: detect the mounted camera card, transfer its NEFs (+ `.xmp` sidecars) to a destination folder with live progress (move or copy, selectable), then eject the card. Skips files already present; ejects only after a zero-error transfer. macOS (`diskutil`) (#48-followup).
- **Byt namn** module: rename NEFs from EXIF `CreateDate` to `YYMMDD_HHMMSS.NEF` (rename_nef GUI), with a preview (dry-run) and confirm. Carries `.xmp` sidecars, disambiguates identical timestamps (`-NN`), skips files without a `CreateDate`, and never overwrites an existing target (restores the original on collision).

### Added
- **Remove redundant encodings** in Database Management: scans each person for redundant encodings — exact byte-identical duplicates, plus near-identical ones above an adjustable threshold — and lists `name: total → kept (N redundant)`, so re-processing bloat can be cleaned per person or all at once. Manual faces are never removed; default threshold `0` removes only exact duplicates. New endpoints `GET /api/v1/management/redundant-encodings` and `POST /api/v1/management/dedup-people` (supports `dry_run`).
- **Find duplicates** in Database Management: scans for pairs of distinctly-named people whose faces look like the same person (centroid cosine distance ≤ an adjustable threshold) and lists them closest-first, so accidental name variants (e.g. "Elis" vs "Elis Niemi") can be reviewed and merged with one click — "Keep A" / "Keep B" merges the other into the kept name. New endpoint `GET /api/v1/management/find-duplicates`; resolution reuses `merge-people`. People with only manual faces (no embedding) are skipped.
- **Twin recognition disambiguation**: at detection time, when a face's top two candidates are a confirmed-distinct pair (e.g. identical twins) and nearly equidistant, the suggested name is re-decided by a k-NN vote over both people's confirmed photos — more robust than the single nearest crop, which can pick the wrong twin. The Review panel shows a "Tvilling-särskiljning → <namn>" hint when this happens. Tunable via `twin_margin` / `twin_knn_k` in `config.json`. Builds on the confirmed-distinct registry below.
- **Twin-aware duplicate detection**: each candidate pair now also reports a head-to-head *separability* — a 1-NN leave-one-out score over both people's confirmed photos. Pairs that are close on centroid but cleanly separable (different people who look alike, e.g. identical twins) are flagged "likely distinct" and sorted last, instead of topping the list. Plus a **"Not a duplicate"** action that records the pair to a persistent confirmed-distinct registry (`distinct_pairs.json`) so future scans skip it, with an "Excluded pairs" list to undo. New endpoints `GET/POST /api/v1/management/distinct-pair(s)` and `POST …/distinct-pair/remove`. (Separability needs ≥2 confirmed photos per person; below that it falls back to centroid distance and the manual exclusion.)
- **Gallra spelare trash filter**: the trash view (Papperskorg) now has a filetype filter (Alla / jpg / nef-raw) so JPEGs and raw files can be reviewed and restored separately. The header shows the filtered-of-total count, and "Töm" empties only the filtered subset when a filter is active (empties everything when set to Alla).
- **Gallra spelare trash retention**: the app-managed culling trash now auto-purges files older than a configurable threshold (default 30 days; `0` = keep forever). Purge runs lazily — at backend startup and whenever the trash view is opened — so the trash can't grow without bound. Configurable under Preferences → Files → Trash (Gallra), persisted as `trash_retention_days` in `config.json`. New endpoints `GET`/`POST /api/v1/culling/retention`.
- **Gallra spelare**: right-click a file for a context menu (navigate, rename, cull, undo) with each action's keyboard shortcut shown inline for discoverability, and a matching "Gallra spelare" section in the shortcuts help (`?`). Navigation extended: `→`/`↓` next, `←`/`↑` previous, `Alt`+arrow pages by 10.
- **Gallra spelare**: rename a file directly from the list — press `Enter` on the selected file (or double-click it) to edit its name inline (Finder-style; the extension is preserved), then `Enter` to commit or `Esc` to cancel. Renames carry `.xmp` sidecars and refuse to overwrite an existing file. Useful when a developed JPG was cropped so a named player is no longer in frame. New endpoint `POST /api/v1/culling/rename`.
- **Gallra spelare** now shows a live per-player count column on the left for the current scope (calls the player-count endpoint), updating immediately as you cull or restore files — so you can see each player's balance shift while you work. The column mirrors the Räkna spelare table — name · count · % · deviation Δ% (signed, color-coded) · distribution bar — with coaches/audience/group and below-threshold names collapsed into an "excluded" section instead of mixed into the counts (the included set now matches `rakna_spelare.py` and the Räkna spelare page exactly). Click a player to filter the list to them (click again to clear).
- **Gallra spelare**: both internal column boundaries are now draggable — the stats column width and the list/preview split — and the widths persist across restarts.

### Changed
- Internal: `ImageViewer` now builds its `file://` image URL via the shared `shared/fileUrl.js` (`toFileUrl`) helper instead of a hand-rolled copy of the same encoding, so the encoding lives in one place. No behavior change (verified byte-identical output across space/`#`/`?`/`[]`, Windows-drive, already-`file://`, and Swedish-filename cases).
- **Resolved the `roadmap` filename collision:** `docs/dev/roadmap.md` is renamed to `docs/dev/performance-plan.md` and the two files' roles are now documented (CLAUDE.md, cross-links in both directions). `ROADMAP.md` (repo root) is the living backlog / known-issues / tech-debt across all horizons; `docs/dev/performance-plan.md` is the narrower release-scoped plan (sprints/deliverables/DoD) for a performance release. The performance plan is no longer pinned to the already-shipped `v1.2.0`. `release-guide.md` reference updated.
- **Renamed `TODO.md` → `ROADMAP.md`** to reflect that it's a forward-looking roadmap, not a done-log: completed items are removed after merge (unless they hang together with pending work), and `CHANGELOG.md` is the durable record of what shipped. All references updated (CLAUDE.md, AGENTS.md, Copilot instructions).
- **`ansikten culling` from the CLI** no longer leaves the face-review panel docked beside culling, and no longer flashes the startup landing page: a CLI launch target skips the landing entirely, and opening culling closes the Review panel (Review is for face review, not culling) while leaving every other open tab in place — it no longer replaces the whole workspace. Review reappears when you enter face review.
- Workflow steps launched from the startup landing page now open in a layout suited to the step: the self-contained modules (Importera · Byt namn · Räkna spelare · Gallra spelare) fill the workspace instead of docking beside the empty Review panel, and "Granska ansikten" opens the review layout.
- Räkna spelare / Gallra spelare filter controls: the primary "Räkna"/"Visa" buttons now use the themed button style (previously rendered as unstyled native buttons), and checkboxes are restyled to match the theme. Changing the player/filetype dropdown, or toggling "Inkl. undermappar" / "Per match", now re-runs immediately — no need to click the button.
- Documentation: established [CLAUDE.md](CLAUDE.md) as the canonical agent-instructions file; [AGENTS.md](AGENTS.md) and `.github/copilot-instructions.md` now point to it.

### Fixed
- **Gallra spelare — rutnätsvyn refreshar en miniatyr efter en in-place om-export**: the overview grid fetched each thumbnail with no cache-key fingerprint, so re-developing a photo to the same path only refreshed the grid after opening the loupe (which already busts via `stat-file-stable`) or restarting. `list_files` now returns a per-file `mtime_ms`/`size` fingerprint (one cheap stat) that `CullingGrid` threads into `useGridThumbnail`, so a single re-export busts exactly that cell (the rest stay cached — the whole grid is not refetched). The overview's blob cache (`gridThumbnailCache.clear()`) is also now freed on culling teardown and when the working set is cleared, instead of living until LRU eviction.
- **Gallra spelare / Räkna spelare — en omladdning (`Cmd+R`) behåller arbetsytan**: a renderer reload wiped the module state, so the loaded folder/omfattning and file list vanished and you had to scan again. The shared scan scope (folders, path-globs, date span, recursion, extension preset) is now persisted for the session (via `sessionStorage`), so after a reload both modules re-adopt it on mount and rebuild the file list automatically. Only the scan scope is restored, not culling's per-player filter. It still resets when the app quits, so a fresh launch (incl. `ansikten culling DIR`) starts clean as before.
- **Gallra spelare — `Enter` is now context-menu-aware like `Esc`**: with the right-click menu open, `Enter` no longer starts an inline rename (or commits a pending name removal) on the current file underneath it. The capture-phase handler now makes `Enter` a true no-op while the menu is open — it still consumes the event (so it can't leak to other modules' Enter handlers, e.g. Review's Enter-to-confirm) but performs no rename/commit — matching how `Esc` already bailed. The mouse-driven menu has no keyboard focus, so no file edit should fire beneath it.
- **Switching views from the menu no longer intermittently does nothing**: opening a module from the app menu (e.g. Database Management while the review layout showed the Image Viewer) could silently land the new tab in the narrow 15% Review column — or, right after a layout loaded, in the first tabset before any panel had been measured — so the switch appeared to do nothing. `openModule` now resolves the target tabset via a shared helper that prefers the active tabset, then the largest *measured* area, and — when nothing is measured yet (the render pass hasn't run) — the highest-*weight* tabset (the main working area). The new tab is also explicitly selected and its tabset made active, so the view always visibly changes. Selection logic extracted to `tabsetUtils.js` (unit-tested for the unmeasured-layout race).
- **Gallra spelare — preview no longer decodes a half-written export (and now refreshes on re-export)**: after re-developing a photo in Lightroom, culling's folder-watch could load the JPEG while it was still being written, decoding it into a thin truncated strip — and because the browser caches `file://` by URL, that broken decode stayed stuck even when navigating away and back. The preview now asks the main process to wait for the file to settle (size + mtime stable) before reading it — but only for recently-modified files, so stepping through old photos stays instant — and cache-busts the `<img>` URL by the file's mtime+size, so an in-place re-export reloads the fresh bytes automatically instead of serving the stale/partial cached copy. New IPC `stat-file-stable`; URL helpers extracted to `src/renderer/shared/fileUrl.js` (unit-tested).
- **Gallra spelare player-filter dropdown now excludes non-players**: the dropdown listed every parsed name verbatim, so group/crowd markers (`Laget`/`FBK`/`Klacken`), coaches, and below-threshold names showed up as filterable "players" — unlike the stats column, which already excludes them. The dropdown now applies the same shared `resolve_exclusion_sets` + `min_images` threshold, so it lists the same player set as `rakna_spelare.py` / the stats column. Files themselves are still listed in full.
- **Gallra spelare — `Esc` discards a file's pending name toggles**: after unchecking names in the preview overlay (row turns orange), there was no way to abandon the change from the main window without either committing it or triggering the navigation dialog. `Esc` now discards the pending toggles for the current file (the row returns to normal) with no dialog. It only acts when culling is the active tabset (so an `Esc` meant for another visible pane can't silently drop culling's edits) and in the capture phase (so it wins over Review's `Esc`); it's a no-op when nothing is pending, leaves the context menu's `Esc` (close menu) alone, and the navigation dialog still owns `Esc` while it's open.
- **Light (beige) theme — status text is legible again**: on the warm beige theme the bright semantic colors (orange/green/red) washed out as small text on the gold backgrounds, so the Δ numbers in the Gallra spelare stats column and the Räkna spelare table (and other status text) were barely readable. Introduced project-wide semantic **status-text** variables — `--color-success-text` / `--color-warning-text` / `--color-error-text` (dark on the light theme, bright on the dark theme) — used wherever status is shown as text: the Δ columns, the global `.text-*` utilities, and `.status-message` text. The hue `--color-*` stays bright for backgrounds/fills/icons (bar fills, status pills, the pending-row highlight). Also unified the data-row states behind shared vars/classes: `--bg-row-hover` (consistent hover that lightens in light mode so dark status text no longer drowns), and a single `.row-selected` highlight (`--bg-row-active` faint accent tint + a left accent bar + bold) reused for every selected/active list row — replacing the culling file list's solid bright-green fill, which read too sharp behind the row text, so the file list and the stats column now mark the current row identically. The culling stats column uses the lighter `--bg-elevated` in light mode. The new vars are defined in both themes and editable in the Theme Editor. The dark theme is unchanged. (Follow-up: the broader `--bg-hover` still darkens in light mode for other components — a separate pass.)
- Rename now keeps manually added faces in the new filename. Manual faces are persisted with the file's content hash (the batch-confirm path previously stored `hash=None`), and the rename name lookup now takes the union of basename- and hash-matched names instead of consulting the hash index only as a fallback — so a manual face anchored by only one key is no longer dropped when an auto-detected face matches by the other. Applies to both the GUI and the legacy CLI rename.
- Rename now holds a file out of name resolution while its review has unsaved changes, closing the ordering window behind the fix above: adding a manual face to an already-processed file and renaming during the brief save window could read the database before the face persisted and drop it from the first-pass filename. The Review panel signals unsaved changes and the file queue excludes such files from rename preview and execution until the save completes.
- Gallra spelare CSS referenced undefined `--border-color` / `--accent` variables, so borders fell back to nothing and the active file row rendered a non-theme blue; it now uses the theme variables (the active row uses the theme accent in both light and dark).
- **Gallra spelare / Räkna spelare deviation colors had poor contrast**: the player tables referenced `--warning`/`--success`/`--error`, which don't exist (the theme defines `--color-warning` etc.), so they fell back to a dull amber — barely legible on the beige theme, and the warn level was invisible on the gold active-row highlight ("yellow on gold"). They now use the real theme colors, and the culling stats active row uses a subtle left-accent-bar + tint instead of a solid gold fill so all three deviation levels stay readable.
- **Undefined CSS variables across several modules**: `ImportModule`, `RenameNefModule`, `ConnectionStatus`, `PlayerCountModule` and `InputBar` referenced non-existent vars (`--border-color`, `--success`/`--warning`/`--error`, `--warning-bg`/`--warning-text`) that fell back to hardcoded off-theme colors (or nothing). They now use the real theme vars (`--border-subtle`, `--color-success`/`-warning`/`-error`, and a theme-aware orange badge for the connection-warning state), so borders and status colors follow the active theme in both light and dark.
- **Gallra spelare keyboard focus traps**: after picking a player in the dropdown, focus stayed on the `<select>` so the next arrow keypress changed the dropdown instead of navigating files; and after a rename (especially `Cmd+Enter` from a name-overlay checkbox) focus lingered on the control so arrows were swallowed. Focus is now handed back to the file list after both, single-key navigation is no longer blocked by a focused overlay checkbox, and the selected row auto-scrolls into view (with ~3 rows of padding) as you navigate.

## [1.3.0] - 2026-06-27

### Added
- Added Review queue overview bar to the Review panel, showing queue progress at a glance.

### Fixed
- Fixed Refine Faces "Preview" failing with `HTTP 404: Not Found`: the request omitted the `/api/v1` prefix the refinement router is mounted under. Also fixed a crash in Shape Repair caused by calling an undefined `setStatus` (now uses `clearStatus`).
- Fixed the Review queue overview bar staying out of sync: queue status is now re-emitted on queue changes.

### Changed
- Simplified the Review queue overview bar by dropping the redundant remaining segment.

### Docs
- Documented the Review queue overview bar in the workspace guide.

## [1.2.0] - 2026-06-20

### Added
- Added file queue filter bar (`/` or `Cmd+F`): filter the file list by filename pattern, with match count indicator. Actions like rename and clear-done scope to filtered items when no checkbox selection exists.

### Fixed
- Fixed Cmd+/Cmd- zoom never stopping: `useKeyHold` now ignores Cmd/Ctrl combos (handled by menu accelerators) and matches keyup by physical key code to avoid stuck animation loops when Shift is released before the base key.
- Fixed menu zoom clicks (Zoom In/Out, Reset, Auto-Fit) having no effect due to stale closures captured at mount time in `useModuleEvent` handlers.
- Fixed first double-click in file queue showing an empty image viewer: `loadFile` now waits for ImageViewer's `load-image` listener before emitting, handling the async gap when `tabEnableRenderOnDemand` causes deferred mounting.
- Fixed `useKeyHold` cleanup using `clearInterval` instead of `cancelAnimationFrame` for rAF-based animation loops.
- Separated focused state from checkbox selection in file queue: plain click highlights an item without checking its checkbox; checkboxes require explicit click, Cmd+Click, or Shift+Click.
- Fixed non-image files (XMP sidecars, etc.) being added to queue from glob patterns. File extension filtering now applied in `addFiles()`, `expandFilePaths()`, and the `expand-glob` IPC handler.

### Changed
- Replaced conda/hardcoded Python paths with convention-based venv discovery (`backend/.venv/`).
- Migrated dependency management from `requirements.txt` to `pyproject.toml` (PEP 621).
- Moved pytest config from `pytest.ini` into `pyproject.toml`.
- Updated CI workflow to install from `pyproject.toml` with optional dependency groups.
- Updated all documentation to use `.venv` and `pip install -e ".[dev]"`.
- Updated release spec references from `bildvisare` to `ansikten`.

### Removed
- Removed obsolete planning and theme example files.

## [1.1.0] - 2026-01-11

### Added
- Added undo stack for face actions and ESC cancel support in review.
- Added backend log streaming to LogViewer over WebSocket and a copy action (`Cmd/Ctrl+A` support).
- Added drag-and-drop support in File Queue.
- Added sidecar file handling in rename flows and sidecar indicators in preview.
- Added JSON Schema for shared type definitions.
- Added pytest and vitest setup with example tests.

### Changed
- Rebranded app naming from Bildvisare/hitta_ansikten to Ansikten across code and documentation.
- Migrated API routes to `/api/v1/` prefix.
- Improved WebSocket reconnect behavior (cap + jitter) and API client offline/network timeout handling.
- Refactored `hitta_ansikten.py` by extracting config, image, and matching modules.
- Consolidated frontend logging via `debug.js` and expanded health endpoint component status.

### Fixed
- Fixed basename collision issues by moving review matching to hash-based logic.
- Fixed rename pipeline to include manual faces and rename only selected files when selection exists.
- Fixed broken thumbnails and missing API version in thumbnail URLs.
- Fixed spammy statistics summary requests and improved auto-refresh stability.
- Fixed multiple ReviewModule UX issues (tab behavior, focus feedback, face index sync, auto-advance edge cases).
- Fixed light theme button hover state and improved theme variables for confirmed/ignored face states.

### Docs
- Updated testing, shortcuts, install/release guides, and rebranding documentation.

### Build
- Added bundle analysis and removed unused dockview dependency.

## [1.0.1] - 2026-01-10

### Changed
- Improved backend packaging by excluding unused dependencies from backend spec.
- Added Electron cache in release workflow for faster CI builds.
- Improved release workflow reliability (skip redundant backend build, fixed PyInstaller onedir copying).
- Improved macOS build handling for symlinks in backend copy step.

### Fixed
- Fixed preprocessing manager state reset when stopping/clearing/renaming.
- Fixed review behavior to avoid auto-advance when no faces are detected.
- Fixed statistics to include additional attempt/resolution data from existing records.

## [1.0.0] - 2026-01-09

- Initial stable release tag.
